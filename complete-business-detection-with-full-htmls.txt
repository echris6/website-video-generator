const businessName = "Luxe Aesthetics & Wellness Spa"; // Test data
const businessDescription = "Premier medical spa offering Botox, fillers, laser treatments";
const businessAddress = "2845 Beverly Hills Boulevard, Beverly Hills, CA 90210";
const businessPhone = "(310) 555-LUXE";

// Enhanced business type detection
function detectBusinessType(name, description) {
    const text = (name + " " + description).toLowerCase();
    
    // Medical Spa Detection
    if (text.includes('med spa') || text.includes('medical spa') || text.includes('aesthetics') || 
        text.includes('botox') || text.includes('filler') || text.includes('laser') || 
        text.includes('spa') && (text.includes('medical') || text.includes('aesthetic'))) {
        return {
            industry: 'Medical Spa',
            template: 'medical_spa',
            primaryColor: '#E8B4CB',
            secondaryColor: '#C891A8',
            backgroundColor: '#FDF8FB',
            textColor: '#2D2D2D',
            accentColor: '#8B5A6B',
            heroTitle: 'Transform Your Beauty Naturally',
            tagline: 'Experience the perfect blend of medical expertise and luxury wellness in our state-of-the-art spa',
            ctaText: 'Schedule Consultation',
            chatbotSelector: '#chatbot-trigger',
            inputSelector: '#chatbot-input'
        };
    }
    
    // Real Estate Detection
    else if (text.includes('real estate') || text.includes('realty') || text.includes('realtor') || 
             text.includes('property') || text.includes('homes') || text.includes('broker')) {
        return {
            industry: 'Real Estate',
            template: 'real_estate',
            primaryColor: '#1a365d',
            secondaryColor: '#d4af37',
            backgroundColor: '#fafafa',
            textColor: '#1a365d',
            accentColor: '#d4af37',
            heroTitle: 'Luxury Property Excellence',
            tagline: 'Navigate the luxury real estate market with unparalleled expertise and exclusive access to premium properties',
            ctaText: 'Schedule Consultation',
            chatbotSelector: '#chatToggle',
            inputSelector: '#messageInput'
        };
    }
    
    // HVAC/Home Services Detection
    else if (text.includes('hvac') || text.includes('heating') || text.includes('cooling') || 
             text.includes('air conditioning') || text.includes('furnace') || text.includes('plumbing') ||
             text.includes('electrical') || text.includes('home service')) {
        return {
            industry: 'HVAC Services',
            template: 'hvac',
            primaryColor: '#1e40af',
            secondaryColor: '#ea580c',
            backgroundColor: '#ffffff',
            textColor: '#1e293b',
            accentColor: '#dc2626',
            heroTitle: 'Professional HVAC Solutions',
            tagline: 'Expert heating, cooling, and ventilation services with fast response times and guaranteed satisfaction',
            ctaText: 'Get Free Estimate',
            chatbotSelector: '#hvacChatToggle',
            inputSelector: '#hvacMessageInput'
        };
    }
    
    // Default HVAC Services (Changed from medical_spa to hvac)
    else {
        return {
            industry: 'HVAC Services',
            template: 'hvac',
            primaryColor: '#1e40af',
            secondaryColor: '#ea580c',
            backgroundColor: '#ffffff',
            textColor: '#1e293b',
            accentColor: '#dc2626',
            heroTitle: 'Professional HVAC Solutions',
            tagline: 'Expert heating, cooling, and ventilation services with fast response times',
            ctaText: 'Get Free Estimate',
            chatbotSelector: '#hvacChatToggle',
            inputSelector: '#hvacMessageInput'
        };
    }
}

// COMPLETE HTML TEMPLATES - THESE GENERATE PROPER PAGE HEIGHTS (3000+ px)
const HTML_TEMPLATES = {
    
    // MEDICAL SPA TEMPLATE - COMPLETE WITH ALL SECTIONS (WORKING - GENERATES 3419px HEIGHT)
    'medical_spa': `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{businessName}} | {{heroTitle}}</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-color: {{primaryColor}};
            --secondary-color: {{secondaryColor}};
            --background-color: {{backgroundColor}};
            --text-color: {{textColor}};
            --accent-color: {{accentColor}};
            
            /* Ultra-luxury color palette */
            --glass-white: rgba(255, 255, 255, 0.1);
            --glass-white-strong: rgba(255, 255, 255, 0.18);
            --glass-dark: rgba(0, 0, 0, 0.1);
            --rose-gold: linear-gradient(135deg, #E8B4B8 0%, #D4A574 50%, #C39BD3 100%);
            --premium-shadow: 0 25px 50px rgba(0, 0, 0, 0.1);
            --premium-shadow-hover: 0 35px 70px rgba(0, 0, 0, 0.15);
            --shimmer: linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.3) 50%, transparent 70%);
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
            line-height: 1.6;
            color: var(--text-color);
            background: var(--background-color);
            overflow-x: hidden;
            position: relative;
        }

        /* Floating Background Elements */
        .floating-elements {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
            overflow: hidden;
        }

        .floating-circle {
            position: absolute;
            border-radius: 50%;
            background: var(--rose-gold);
            opacity: 0.1;
            animation: float 20s infinite ease-in-out;
        }

        .floating-circle:nth-child(1) {
            width: 300px;
            height: 300px;
            top: 10%;
            left: 10%;
            animation-delay: 0s;
        }

        .floating-circle:nth-child(2) {
            width: 200px;
            height: 200px;
            top: 60%;
            right: 15%;
            animation-delay: -5s;
        }

        .floating-circle:nth-child(3) {
            width: 150px;
            height: 150px;
            bottom: 20%;
            left: 20%;
            animation-delay: -10s;
        }

        .floating-circle:nth-child(4) {
            width: 250px;
            height: 250px;
            top: 30%;
            right: 30%;
            animation-delay: -15s;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            25% { transform: translateY(-20px) rotate(90deg); }
            50% { transform: translateY(-40px) rotate(180deg); }
            75% { transform: translateY(-20px) rotate(270deg); }
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Reveal Animation */
        .reveal-element {
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .reveal-element.revealed {
            opacity: 1;
            transform: translateY(0);
        }

        /* Premium Header */
        header {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border-bottom: 1px solid var(--glass-white-strong);
            padding: 1.5rem 0;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        header.scrolled {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            box-shadow: var(--premium-shadow);
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-family: 'Inter', sans-serif;
            font-size: 1.8rem;
            font-weight: 200;
            background: var(--rose-gold);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 0.02em;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 3rem;
        }

        nav a {
            text-decoration: none;
            color: var(--text-color);
            font-weight: 300;
            font-size: 1rem;
            position: relative;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        nav a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--rose-gold);
            transition: width 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        nav a:hover::after {
            width: 100%;
        }

        nav a:hover {
            color: var(--primary-color);
        }

        /* Cinematic Hero Section */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            background: linear-gradient(135deg, var(--background-color) 0%, rgba(248, 249, 251, 0.8) 100%);
            padding-top: 120px;
            position: relative;
            overflow: hidden;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 30% 20%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
            pointer-events: none;
        }

        .hero-content {
            max-width: 900px;
            position: relative;
            z-index: 2;
        }

        .hero h1 {
            font-family: 'Inter', sans-serif;
            font-size: clamp(3rem, 6vw, 5rem);
            font-weight: 200;
            letter-spacing: -0.02em;
            margin-bottom: 2rem;
            background: var(--rose-gold);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            line-height: 1.1;
            position: relative;
        }

        .hero p {
            font-size: 1.4rem;
            color: #6B7280;
            margin-bottom: 4rem;
            font-weight: 300;
            line-height: 1.7;
            max-width: 600px;
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            gap: 1rem;
            background: var(--rose-gold);
            color: white;
            padding: 1.5rem 4rem;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 400;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            overflow: hidden;
            box-shadow: var(--premium-shadow);
            backdrop-filter: blur(10px);
            -webkit-backdrop-filter: blur(10px);
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--shimmer);
            transition: left 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .cta-button:hover::before {
            left: 100%;
        }

        .cta-button:hover {
            transform: translateY(-5px);
            box-shadow: var(--premium-shadow-hover);
        }

        /* Premium Services Section */
        .services {
            padding: 10rem 0;
            position: relative;
        }

        .services h2 {
            text-align: center;
            font-family: 'Inter', sans-serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 200;
            margin-bottom: 6rem;
            background: var(--rose-gold);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.01em;
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
            max-width: 1100px;
            margin: 0 auto;
        }

        .service-card {
            background: var(--glass-white);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-white-strong);
            padding: 1.8rem 1.2rem;
            border-radius: 20px;
            text-align: center;
            box-shadow: var(--premium-shadow);
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            overflow: hidden;
            will-change: transform;
            max-width: 350px;
            margin: 0 auto;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--rose-gold);
            opacity: 0;
            transition: opacity 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            border-radius: 24px;
        }

        .service-card:hover::before {
            opacity: 0.05;
        }

        .service-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--premium-shadow-hover);
            border-color: rgba(255, 255, 255, 0.3);
        }

        .service-image {
            width: 100%;
            height: 150px;
            margin-bottom: 1.5rem;
            border-radius: 12px;
            overflow: hidden;
            position: relative;
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.1);
        }

        .service-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            object-position: center;
            border-radius: 12px;
            transition: transform 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .service-card:hover .service-image img {
            transform: scale(1.05);
        }

        .service-card h3 {
            font-family: 'Inter', sans-serif;
            font-size: 1.5rem;
            font-weight: 300;
            margin-bottom: 1.5rem;
            color: var(--primary-color);
            position: relative;
            z-index: 2;
        }

        .service-card p {
            font-size: 1rem;
            color: #6B7280;
            margin-bottom: 2rem;
            line-height: 1.6;
            position: relative;
            z-index: 2;
        }

        .service-price {
            font-size: 1.2rem;
            font-weight: 400;
            background: var(--rose-gold);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 2rem;
            position: relative;
            z-index: 2;
        }

        .service-cta {
            background: transparent;
            color: white;
            border: 2px solid;
            border-image: var(--rose-gold) 1;
            padding: 0.8rem 1.8rem;
            border-radius: 25px;
            font-size: 0.9rem;
            font-weight: 500;
            font-family: 'Inter', sans-serif;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            z-index: 2;
            overflow: hidden;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
            text-transform: none;
            letter-spacing: 0.01em;
            border: 2px solid transparent;
            background-clip: padding-box;
            margin-top: 0.5rem;
            display: inline-block;
        }

        .service-cta::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--rose-gold);
            border-radius: 25px;
            opacity: 0;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            z-index: -1;
        }

        .service-cta::after {
            content: '';
            position: absolute;
            top: -2px;
            left: -2px;
            right: -2px;
            bottom: -2px;
            background: var(--rose-gold);
            border-radius: 27px;
            z-index: -2;
        }

        .service-cta:hover::before {
            opacity: 1;
        }

        .service-cta:hover {
            color: white;
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0, 0, 0, 0.15);
        }

        /* Luxury Contact Section */
        .contact {
            padding: 10rem 0;
            background: linear-gradient(135deg, #F8F9FB 0%, rgba(248, 249, 251, 0.5) 100%);
            position: relative;
        }

        .contact::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 70% 80%, rgba(255, 255, 255, 0.1) 0%, transparent 50%);
            pointer-events: none;
        }

        .contact h2 {
            text-align: center;
            font-family: 'Inter', sans-serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 200;
            margin-bottom: 4rem;
            background: var(--rose-gold);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.01em;
        }

        .contact-content {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 3rem;
            max-width: 1200px;
            margin: 0 auto;
            align-items: stretch;
        }

        .map-container {
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.15);
            border-radius: 20px;
            box-shadow: var(--premium-shadow);
            overflow: hidden;
            position: relative;
            height: 600px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .map-container::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--rose-gold);
            opacity: 0.02;
            pointer-events: none;
        }

        .map-embed {
            width: 100%;
            height: 100%;
            border: none;
            border-radius: 20px;
        }

        .map-placeholder {
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 2rem;
            color: var(--text-color);
            height: 100%;
        }

        .map-placeholder .map-icon {
            font-size: 4rem;
            margin-bottom: 2rem;
            display: block;
            opacity: 0.8;
        }

        .map-placeholder h3 {
            font-family: 'Inter', sans-serif;
            font-size: 1.8rem;
            font-weight: 300;
            margin-bottom: 1.5rem;
            background: var(--rose-gold);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .map-placeholder p {
            font-size: 1.1rem;
            line-height: 1.6;
            color: #6B7280;
            font-weight: 300;
            margin-bottom: 0.5rem;
        }

        .map-placeholder p:last-child {
            margin-bottom: 0;
        }

        .contact-form {
            background: rgba(255, 255, 255, 0.08);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.15);
            padding: 2rem;
            border-radius: 20px;
            box-shadow: var(--premium-shadow);
            position: relative;
            z-index: 2;
            height: 600px;
            display: flex;
            flex-direction: column;
        }

        .form-group {
            margin-bottom: 1.5rem;
            position: relative;
            flex: 0 0 auto;
        }

        .form-group:last-of-type {
            flex: 1;
            display: flex;
            flex-direction: column;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 400;
            color: var(--text-color);
            font-size: 0.85rem;
            letter-spacing: 0.01em;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 1rem;
            border: 2px solid rgba(229, 231, 235, 0.25);
            border-radius: 12px;
            font-size: 0.9rem;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            font-family: inherit;
            background: rgba(255, 255, 255, 0.6);
            backdrop-filter: blur(8px);
            -webkit-backdrop-filter: blur(8px);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--primary-color);
            transform: translateY(-2px);
            box-shadow: 0 6px 16px rgba(0, 0, 0, 0.06);
        }

        .form-group textarea {
            flex: 1;
            min-height: 100px;
            resize: vertical;
        }

        .contact-form .cta-button {
            margin-top: auto;
            align-self: flex-start;
        }



        /* Sophisticated Footer */
        footer {
            background: linear-gradient(135deg, var(--text-color) 0%, rgba(31, 41, 55, 0.95) 100%);
            color: white;
            text-align: center;
            padding: 4rem 0;
            position: relative;
        }

        footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--rose-gold);
            opacity: 0.05;
        }

        footer p {
            margin-bottom: 1rem;
            font-weight: 300;
        }

        /* Responsive Excellence */
        @media (max-width: 1024px) {
            .container {
                padding: 0 1.5rem;
            }
            
                         .services-grid {
                 grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
                 gap: 2rem;
                 max-width: 900px;
             }
             
             .service-card {
                 max-width: none;
             }
             
             .floating-circle {
                 opacity: 0.05;
             }
        }

        @media (max-width: 768px) {
            .container {
                padding: 0 1rem;
            }

            nav ul {
                gap: 1.5rem;
            }

            nav a {
                font-size: 0.9rem;
            }

            .hero {
                padding-top: 120px;
            }

            .hero-content {
                text-align: center;
            }

            .hero h1 {
                font-size: 2.5rem;
                margin-bottom: 1.5rem;
            }

            .hero p {
                font-size: 1.2rem;
                margin-bottom: 3rem;
            }

            .services {
                padding: 6rem 0;
            }

            .services h2 {
                margin-bottom: 4rem;
            }

            .services-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
                max-width: 100%;
            }

            .service-card {
                padding: 2rem 1.5rem;
                max-width: none;
            }

            .service-image {
                height: 130px;
                margin-bottom: 1.2rem;
            }

            .service-image img {
                border-radius: 10px;
            }

            .service-card h3 {
                font-size: 1.3rem;
                margin-bottom: 1.2rem;
            }

            .service-card p {
                font-size: 0.95rem;
                margin-bottom: 1.5rem;
            }

            .service-price {
                font-size: 1.1rem;
                margin-bottom: 1.5rem;
            }

            .service-cta {
                padding: 0.7rem 1.6rem;
                font-size: 0.85rem;
                border-radius: 22px;
            }

            .service-cta::after {
                border-radius: 24px;
            }

             .contact {
                 padding: 6rem 0;
             }

             .contact h2 {
                 margin-bottom: 3rem;
             }

             .contact-content {
                 gap: 2rem;
             }

             .map-container,
             .contact-form {
                 height: 500px;
             }

             .map-placeholder {
                 padding: 1.8rem;
             }

             .map-placeholder .map-icon {
                 font-size: 3.5rem;
                 margin-bottom: 1.5rem;
             }

             .map-placeholder h3 {
                 font-size: 1.5rem;
                 margin-bottom: 1.2rem;
             }

             .map-placeholder p {
                 font-size: 1rem;
             }

             .contact-form {
                 padding: 1.8rem;
             }

             .form-group {
                 margin-bottom: 1.3rem;
             }

             .form-group input,
             .form-group textarea {
                 padding: 0.9rem;
                 font-size: 0.85rem;
             }

             .form-group textarea {
                 min-height: 80px;
             }



            .floating-circle {
                display: none;
            }
        }

        @media (max-width: 480px) {
            .hero h1 {
                font-size: 2rem;
            }

            .cta-button {
                padding: 1.2rem 2.5rem;
                font-size: 1rem;
            }

            .service-card {
                padding: 1.5rem 1rem;
            }

            .service-card h3 {
                font-size: 1.2rem;
                margin-bottom: 1rem;
            }

            .service-card p {
                font-size: 0.9rem;
                margin-bottom: 1.2rem;
            }

            .service-price {
                font-size: 1rem;
                margin-bottom: 1.2rem;
            }

            .service-cta {
                padding: 0.7rem 1.4rem;
                font-size: 0.8rem;
                border-radius: 20px;
            }

            .service-cta::before {
                border-radius: 20px;
            }

            .service-cta::after {
                border-radius: 22px;
            }

            .service-image {
                height: 120px;
                margin-bottom: 1rem;
            }

            .service-image img {
                border-radius: 8px;
            }

            .contact-content {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .map-container {
                height: 350px;
                order: 1;
            }

            .map-placeholder {
                padding: 1.5rem;
            }

            .map-placeholder .map-icon {
                font-size: 3rem;
                margin-bottom: 1.2rem;
            }

            .map-placeholder h3 {
                font-size: 1.4rem;
                margin-bottom: 1rem;
            }

            .map-placeholder p {
                font-size: 0.95rem;
            }

            .contact-form {
                height: 400px;
                padding: 1.5rem;
                order: 2;
            }

            .form-group {
                margin-bottom: 1.2rem;
            }

            .form-group input,
            .form-group textarea {
                padding: 0.9rem;
                font-size: 0.85rem;
            }

            .form-group textarea {
                min-height: 70px;
            }
        }

        /* Performance Optimizations */
        .service-card,
        .cta-button {
            will-change: transform;
        }

        /* Accessibility Improvements */
        @media (prefers-reduced-motion: reduce) {
            *,
            *::before,
            *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
            
            .floating-circle {
                animation: none;
            }
        }

        /* Simple Medical Spa Chatbot */
        .chatbot-widget {
            position: fixed;
            bottom: 30px;
            right: 30px;
            z-index: 9999;
            font-family: 'Inter', sans-serif;
        }

        .chatbot-trigger {
            width: 60px;
            height: 60px;
            background: var(--rose-gold);
            border-radius: 50%;
            border: none;
            cursor: pointer;
            box-shadow: var(--premium-shadow);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-size: 24px;
            transition: all 0.3s ease;
        }

        .chatbot-trigger:hover {
            transform: scale(1.05);
            box-shadow: var(--premium-shadow-hover);
        }

        .chatbot-container {
            position: absolute;
            bottom: 80px;
            right: 0;
            width: 350px;
            height: 450px;
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 16px;
            box-shadow: var(--premium-shadow);
            display: none;
            flex-direction: column;
            overflow: hidden;
        }

        .chatbot-container.active {
            display: flex;
        }

        .chatbot-header {
            background: var(--rose-gold);
            color: white;
            padding: 16px 20px;
            text-align: center;
            position: relative;
        }

        .chatbot-header h3 {
            margin: 0;
            font-size: 1.1rem;
            font-weight: 500;
        }

        .chatbot-close {
            position: absolute;
            top: 12px;
            right: 15px;
            background: none;
            border: none;
            color: white;
            font-size: 20px;
            cursor: pointer;
            width: 24px;
            height: 24px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .chatbot-messages {
            flex: 1;
            padding: 20px;
            overflow-y: auto;
            background: rgba(255, 255, 255, 0.02);
        }

        .message {
            margin-bottom: 12px;
            padding: 8px 12px;
            border-radius: 12px;
            font-size: 0.9rem;
            line-height: 1.4;
        }

        .message.bot {
            background: #f8f9fa;
            border: 1px solid #e9ecef;
            color: #333;
            border-radius: 12px 12px 12px 4px;
        }

        .message.user {
            background: var(--rose-gold);
            color: white;
            border-radius: 12px 12px 4px 12px;
            margin-left: 30px;
        }

        .chatbot-input-area {
            padding: 15px 20px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .chatbot-input {
            width: 100%;
            padding: 10px 15px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            background: var(--glass-white);
            backdrop-filter: blur(10px);
            color: #333;
            font-size: 0.9rem;
            outline: none;
            transition: all 0.3s ease;
        }

        .chatbot-input:focus {
            border-color: rgba(232, 180, 184, 0.5);
            background: rgba(255, 255, 255, 0.3);
        }

        .chatbot-input::placeholder {
            color: rgba(51, 51, 51, 0.6);
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .chatbot-widget {
                bottom: 20px;
                right: 20px;
            }

            .chatbot-container {
                width: 300px;
                height: 400px;
            }

            .chatbot-trigger {
                width: 55px;
                height: 55px;
                font-size: 20px;
            }
        }

        @media (max-width: 480px) {
            .chatbot-container {
                width: calc(100vw - 40px);
                right: -10px;
                height: 350px;
            }
        }
    </style>
</head>
<body>
    <!-- Floating Background Elements -->
    <div class="floating-elements">
        <div class="floating-circle"></div>
        <div class="floating-circle"></div>
        <div class="floating-circle"></div>
        <div class="floating-circle"></div>
    </div>

    <!-- Premium Header -->
    <header id="header">
        <div class="container">
            <div class="header-content">
                <div class="logo">{{businessName}}</div>
                <nav>
                    <ul>
                        <li><a href="#home">Home</a></li>
                        <li><a href="#services">Services</a></li>
                        <li><a href="#contact">Contact</a></li>
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <!-- Cinematic Hero Section -->
    <section class="hero reveal-element" id="home">
        <div class="container">
            <div class="hero-content">
                <h1>{{heroTitle}}</h1>
                <p>{{tagline}}</p>
                <a href="#services" class="cta-button">{{ctaText}}</a>
            </div>
        </div>
    </section>

    <!-- Premium Services Section -->
    <section class="services" id="services">
        <div class="container">
            <h2 class="reveal-element">Signature Treatments</h2>
            <div class="services-grid reveal-element">
                <!-- Service Card 1: Botox & Dermal Fillers -->
                <div class="service-card">
                    <div class="service-image">
                        <img src="card1.png" alt="Botox & Dermal Fillers" />
                    </div>
                    <h3>Botox & Dermal Fillers</h3>
                    <p>Professional injectable treatments to smooth wrinkles and enhance facial contours with natural-looking results</p>
                    <div class="service-price">From \$295</div>
                    <button class="service-cta">Book Consultation</button>
                </div>

                <!-- Service Card 2: Laser Hair Removal -->
                <div class="service-card">
                    <div class="service-image">
                        <img src="card2.png" alt="Laser Hair Removal" />
                    </div>
                    <h3>Laser Hair Removal</h3>
                    <p>Advanced laser technology for safe, effective, and permanent hair reduction on all skin types</p>
                    <div class="service-price">From \$195</div>
                    <button class="service-cta">Book Consultation</button>
                </div>

                <!-- Service Card 3: HydraFacial Rejuvenation -->
                <div class="service-card">
                    <div class="service-image">
                        <img src="card3.png" alt="HydraFacial Rejuvenation" />
                    </div>
                    <h3>HydraFacial Rejuvenation</h3>
                    <p>Multi-step facial treatment that cleanses, extracts, and hydrates for instantly glowing skin</p>
                    <div class="service-price">From \$175</div>
                    <button class="service-cta">Book Consultation</button>
                </div>
            </div>
        </div>
    </section>

    <!-- Luxury Contact Section -->
    <section class="contact" id="contact">
        <div class="container">
            <h2 class="reveal-element">Begin Your Journey</h2>
            <div class="contact-content reveal-element">
                <!-- Map Container -->
                <div class="map-container">
                    <div class="map-embed">
                        {{mapEmbedCode}}
                    </div>
                    <div class="map-placeholder">
                        <span class="map-icon">📍</span>
                        <h3>Visit Our Clinic</h3>
                        <p>{{businessAddress}}</p>
                        <p>{{businessPhone}}</p>
                    </div>
                </div>

                <!-- Contact Form -->
                <form class="contact-form">
                    <div class="form-group">
                        <label for="name">Full Name</label>
                        <input type="text" id="name" name="name" required>
                    </div>
                    <div class="form-group">
                        <label for="email">Email Address</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="phone">Phone Number</label>
                        <input type="tel" id="phone" name="phone">
                    </div>
                    <div class="form-group">
                        <label for="message">Treatment Interests</label>
                        <textarea id="message" name="message" placeholder="Tell us about your aesthetic goals..."></textarea>
                    </div>
                    <button type="submit" class="cta-button">{{ctaText}}</button>
                </form>
            </div>
        </div>
    </section>



    <!-- Sophisticated Footer -->
    <footer>
        <div class="container">
            <p>&copy; 2024 {{businessName}}. All rights reserved.</p>
            <p>{{businessAddress}} | {{businessPhone}}</p>
        </div>
    </footer>

    <!-- Simple Chatbot Widget -->
    <div class="chatbot-widget">
        <button class="chatbot-trigger" id="chatbot-trigger">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                <path d="M21 11.5C21.0034 12.8199 20.6951 14.1219 20.1 15.3C19.3944 16.7118 18.3098 17.8992 16.9674 18.7293C15.6251 19.5594 14.0782 19.9994 12.5 20C11.1801 20.0035 9.87812 19.6951 8.7 19.1L3 21L4.9 15.3C4.30493 14.1219 3.99656 12.8199 4 11.5C4.00061 9.92179 4.44061 8.37488 5.27072 7.03258C6.10083 5.69028 7.28825 4.6056 8.7 3.90003C9.87812 3.30496 11.1801 2.99659 12.5 3.00003H13C15.0843 3.11502 17.053 3.99479 18.5291 5.47089C20.0052 6.94699 20.885 8.91568 21 11V11.5Z" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/>
            </svg>
        </button>
        <div class="chatbot-container" id="chatbot-container">
            <div class="chatbot-header">
                <button class="chatbot-close" id="chatbot-close">×</button>
                <h3>Ask Us Anything</h3>
            </div>
            
            <div class="chatbot-messages" id="chatbot-messages">
                <div class="message bot">
                    Hi! I can answer questions about our treatments, pricing, and hours. What would you like to know?
                </div>
            </div>
            
            <div class="chatbot-input-area">
                <input type="text" class="chatbot-input" id="chatbot-input" placeholder="Ask a question...">
            </div>
        </div>
    </div>



    <!-- Advanced JavaScript for Premium Interactions -->
    <script>
        // Smooth Header Scroll Effect
        window.addEventListener('scroll', () => {
            const header = document.getElementById('header');
            if (window.scrollY > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });

        // Intersection Observer for Reveal Animations
        const revealElements = document.querySelectorAll('.reveal-element');
        const revealObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('revealed');
                }
            });
        }, {
            threshold: 0.1,
            rootMargin: '0px 0px -50px 0px'
        });

        revealElements.forEach(element => {
            revealObserver.observe(element);
        });



        // Smooth Scroll for Navigation Links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({
                        behavior: 'smooth',
                        block: 'start'
                    });
                }
            });
        });

        // Premium Form Interactions
        const formInputs = document.querySelectorAll('input, textarea');
        formInputs.forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.style.transform = 'translateY(-2px)';
            });
            
            input.addEventListener('blur', function() {
                this.parentElement.style.transform = 'translateY(0)';
            });
        });

        // Service Card Hover Effects
        const serviceCards = document.querySelectorAll('.service-card');
        serviceCards.forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.willChange = 'transform';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.willChange = 'auto';
            });
        });

        // Map Display Logic
        function handleMapDisplay() {
            const mapEmbed = document.querySelector('.map-embed');
            const mapPlaceholder = document.querySelector('.map-placeholder');
            
            // Check if map embed code exists and has content
            if (mapEmbed && mapEmbed.innerHTML.trim() && mapEmbed.innerHTML.trim() !== '{{mapEmbedCode}}') {
                mapEmbed.style.display = 'block';
                mapPlaceholder.style.display = 'none';
            } else {
                mapEmbed.style.display = 'none';
                mapPlaceholder.style.display = 'flex';
                mapPlaceholder.style.flexDirection = 'column';
                mapPlaceholder.style.alignItems = 'center';
                mapPlaceholder.style.justifyContent = 'center';
            }
        }

        // Initialize map display
        handleMapDisplay();

        // Performance Optimization
        if ('requestIdleCallback' in window) {
            requestIdleCallback(() => {
                // Preload any heavy resources here
                console.log('Premium medical spa experience ready');
            });
        }



        // Simple Chatbot Functionality
        class SimpleChatbot {
            constructor() {
                this.trigger = document.getElementById('chatbot-trigger');
                this.container = document.getElementById('chatbot-container');
                this.closeBtn = document.getElementById('chatbot-close');
                this.messagesContainer = document.getElementById('chatbot-messages');
                this.chatInput = document.getElementById('chatbot-input');
                this.isOpen = false;
                
                this.responses = {
                    greeting: "Hi! I can help you with information about our treatments and services.",
                    botox: "Botox reduces wrinkles and costs \$295+ per area. Results last 3-4 months.",
                    fillers: "Dermal fillers add volume and smooth lines. Starting at \$495 per syringe.",
                    laser: "Laser hair removal provides permanent results. \$195+ per session.",
                    hydrafacial: "HydraFacial cleanses and hydrates skin. \$175 per treatment.",
                    pricing: "Treatment prices: HydraFacial \$175, Laser \$195+, Botox \$295+, Fillers \$495+",
                    hours: "We're open Monday-Friday 9am-6pm, Saturday 9am-4pm. Closed Sundays.",
                    booking: "To book an appointment, please call us or use our contact form on the website.",
                    default: "I can help with questions about treatments, pricing, hours, or booking appointments. What would you like to know?"
                };
                
                this.init();
            }
            
            init() {
                this.trigger.addEventListener('click', () => this.toggle());
                this.closeBtn.addEventListener('click', () => this.close());
                this.chatInput.addEventListener('keypress', (e) => {
                    if (e.key === 'Enter') this.handleUserMessage();
                });
            }
            
            toggle() {
                if (this.isOpen) {
                    this.close();
                } else {
                    this.open();
                }
            }
            
            open() {
                this.container.classList.add('active');
                this.isOpen = true;
            }
            
            close() {
                this.container.classList.remove('active');
                this.isOpen = false;
            }
            
            addMessage(message, isUser = false) {
                const messageDiv = document.createElement('div');
                messageDiv.className = \`message \${isUser ? 'user' : 'bot'}\`;
                messageDiv.textContent = message;
                this.messagesContainer.appendChild(messageDiv);
                this.messagesContainer.scrollTop = this.messagesContainer.scrollHeight;
            }
            
            handleUserMessage() {
                const message = this.chatInput.value.trim();
                if (!message) return;
                
                this.addMessage(message, true);
                this.chatInput.value = '';
                
                setTimeout(() => {
                    const response = this.generateResponse(message);
                    this.addMessage(response);
                }, 500);
            }
            
            generateResponse(message) {
                const lowerMessage = message.toLowerCase();
                
                if (lowerMessage.includes('hi') || lowerMessage.includes('hello')) {
                    return this.responses.greeting;
                } else if (lowerMessage.includes('botox')) {
                    return this.responses.botox;
                } else if (lowerMessage.includes('filler')) {
                    return this.responses.fillers;
                } else if (lowerMessage.includes('laser') || lowerMessage.includes('hair')) {
                    return this.responses.laser;
                } else if (lowerMessage.includes('hydrafacial') || lowerMessage.includes('facial')) {
                    return this.responses.hydrafacial;
                } else if (lowerMessage.includes('price') || lowerMessage.includes('cost')) {
                    return this.responses.pricing;
                } else if (lowerMessage.includes('hours') || lowerMessage.includes('open')) {
                    return this.responses.hours;
                } else if (lowerMessage.includes('book') || lowerMessage.includes('appointment') || lowerMessage.includes('schedule')) {
                    return this.responses.booking;
                } else {
                    return this.responses.default;
                }
            }
        }
        
        // Initialize the simple chatbot
        const simpleChatbot = new SimpleChatbot();
    </script>
</body>
</html>`,
    
    // REAL ESTATE TEMPLATE - COMPLETE WITH ALL SECTIONS  
    'real_estate': `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{businessName}} | {{heroTitle}}</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-color: {{primaryColor}};
            --secondary-color: {{secondaryColor}};
            --background-color: {{backgroundColor}};
            --text-color: {{textColor}};
            --accent-color: {{accentColor}};
            
            /* Ultra-luxury Sotheby's color palette */
            --navy-blue: #0a1929;
            --navy-blue-light: #1a365d;
            --navy-blue-deep: #051922;
            --executive-navy: #0f2027;
            --gold-accent: linear-gradient(135deg, #c9a96e 0%, #f4e7a1 25%, #d4af37 50%, #b8860b 75%, #c9a96e 100%);
            --gold-metallic: linear-gradient(145deg, #ffd700 0%, #ffed4a 15%, #d4af37 35%, #b8860b 55%, #8b6914 75%, #c9a96e 100%);
            --platinum-gradient: linear-gradient(135deg, #e5e7eb 0%, #f8fafc 25%, #ffffff 50%, #f1f5f9 75%, #e2e8f0 100%);
            --architectural-white: #fdfdfd;
            --executive-gray: #2d3748;
            --platinum: #e2e8f0;
            --marble-white: linear-gradient(135deg, #ffffff 0%, #f8fafc 50%, #f1f5f9 100%);
            
            /* Advanced glass-morphism layers */
            --glass-ultra: rgba(255, 255, 255, 0.05);
            --glass-light: rgba(255, 255, 255, 0.08);
            --glass-medium: rgba(255, 255, 255, 0.12);
            --glass-strong: rgba(255, 255, 255, 0.18);
            --glass-executive: rgba(255, 255, 255, 0.25);
            
            /* Sophisticated shadow system */
            --shadow-subtle: 0 4px 20px rgba(10, 25, 41, 0.08);
            --shadow-medium: 0 8px 30px rgba(10, 25, 41, 0.12);
            --shadow-strong: 0 15px 40px rgba(10, 25, 41, 0.15);
            --shadow-executive: 0 25px 60px rgba(10, 25, 41, 0.18);
            --shadow-luxury: 0 35px 80px rgba(10, 25, 41, 0.22);
            
            /* Premium effects */
            --shimmer: linear-gradient(45deg, transparent 25%, rgba(255, 255, 255, 0.4) 50%, transparent 75%);
            --shimmer-gold: linear-gradient(45deg, transparent 25%, rgba(255, 215, 0, 0.3) 50%, transparent 75%);
            --parallax-speed: 0.5;
            
            /* Executive spacing system */
            --spacing-xs: 0.5rem;
            --spacing-sm: 1rem;
            --spacing-md: 2rem;
            --spacing-lg: 4rem;
            --spacing-xl: 6rem;
            --spacing-xxl: 10rem;
            
            /* Typography system */
            --font-primary: 'Inter', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
            --font-display: 'Inter', serif;
            --weight-ultra-light: 100;
            --weight-light: 200;
            --weight-normal: 300;
            --weight-medium: 400;
            --weight-semi-bold: 500;
            --weight-bold: 600;
            --weight-black: 700;
        }

        html {
            scroll-behavior: smooth;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
            line-height: 1.6;
            color: var(--navy-blue);
            background: var(--architectural-white);
            overflow-x: hidden;
            position: relative;
        }

        /* Architectural Blueprint Elements */
        .floating-elements {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
            overflow: hidden;
            perspective: 1200px;
        }

        .floating-square {
            position: absolute;
            border: 2px solid var(--gold-accent);
            background: transparent;
            opacity: 0.12;
            animation: architecturalFloat 30s infinite ease-in-out;
            transform-style: preserve-3d;
        }

        .floating-square::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 60%;
            height: 60%;
            border: 1px solid var(--gold-accent);
            transform: translate(-50%, -50%);
            opacity: 0.6;
        }

        .floating-square::after {
            content: '';
            position: absolute;
            top: 20%;
            left: 20%;
            width: 20%;
            height: 20%;
            background: var(--gold-accent);
            border-radius: 50%;
            opacity: 0.4;
        }

        .floating-square:nth-child(1) {
            width: 240px;
            height: 160px;
            top: 12%;
            left: 5%;
            animation-delay: 0s;
            border-radius: 12px;
        }

        .floating-square:nth-child(2) {
            width: 180px;
            height: 180px;
            top: 45%;
            right: 8%;
            animation-delay: -10s;
            border-radius: 50%;
        }

        .floating-square:nth-child(3) {
            width: 140px;
            height: 200px;
            bottom: 20%;
            left: 20%;
            animation-delay: -20s;
            border-radius: 8px;
        }

        .floating-square:nth-child(4) {
            width: 220px;
            height: 140px;
            top: 28%;
            right: 30%;
            animation-delay: -15s;
            border-radius: 16px;
        }

        .floating-square:nth-child(5) {
            width: 100px;
            height: 100px;
            top: 70%;
            right: 15%;
            animation-delay: -25s;
            border-radius: 50%;
        }

        .floating-square:nth-child(6) {
            width: 160px;
            height: 120px;
            bottom: 35%;
            right: 50%;
            animation-delay: -5s;
            border-radius: 10px;
        }

        @keyframes architecturalFloat {
            0%, 100% { 
                transform: translateY(0px) translateX(0px) rotateX(0deg) rotateY(0deg) rotateZ(0deg); 
                opacity: 0.12; 
            }
            16% { 
                transform: translateY(-20px) translateX(10px) rotateX(5deg) rotateY(0deg) rotateZ(2deg); 
                opacity: 0.18; 
            }
            33% { 
                transform: translateY(-35px) translateX(0px) rotateX(0deg) rotateY(5deg) rotateZ(0deg); 
                opacity: 0.15; 
            }
            50% { 
                transform: translateY(-25px) translateX(-10px) rotateX(-3deg) rotateY(0deg) rotateZ(-2deg); 
                opacity: 0.20; 
            }
            66% { 
                transform: translateY(-40px) translateX(5px) rotateX(0deg) rotateY(-3deg) rotateZ(1deg); 
                opacity: 0.16; 
            }
            83% { 
                transform: translateY(-15px) translateX(-5px) rotateX(2deg) rotateY(0deg) rotateZ(-1deg); 
                opacity: 0.14; 
            }
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Reveal Animation */
        .reveal-element {
            opacity: 0;
            transform: translateY(50px);
            transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .reveal-element.revealed {
            opacity: 1;
            transform: translateY(0);
        }

        /* Ultra-Executive Header */
        header {
            background: var(--glass-light);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border-bottom: 1px solid var(--glass-strong);
            padding: var(--spacing-sm) 0;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            transition: all 0.5s cubic-bezier(0.23, 1, 0.32, 1);
        }

        header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--marble-white);
            opacity: 0.85;
            z-index: -1;
        }

        header.scrolled {
            background: var(--glass-executive);
            backdrop-filter: blur(40px);
            -webkit-backdrop-filter: blur(40px);
            box-shadow: var(--shadow-executive);
            border-bottom-color: rgba(201, 169, 110, 0.2);
        }

        header.scrolled::before {
            opacity: 0.95;
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-family: 'Inter', sans-serif;
            font-size: 1.8rem;
            font-weight: 300;
            background: linear-gradient(135deg, #c8a882 0%, #d4af37 50%, #b8860b 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: 0.02em;
        }

        nav ul {
            display: flex;
            list-style: none;
            gap: 3rem;
        }

        nav a {
            text-decoration: none;
            color: var(--navy-blue);
            font-weight: 400;
            font-size: 1rem;
            position: relative;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        nav a::after {
            content: '';
            position: absolute;
            bottom: -5px;
            left: 0;
            width: 0;
            height: 2px;
            background: var(--gold-accent);
            transition: width 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        nav a:hover::after {
            width: 100%;
        }

        nav a:hover {
            color: #d4af37;
        }

        /* Ultra-Executive Hero Section with Parallax */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            background: var(--marble-white);
            padding-top: 120px;
            position: relative;
            overflow: hidden;
            perspective: 1000px;
        }

        .hero::before {
            content: '';
            position: absolute;
            top: -20%;
            left: -20%;
            right: -20%;
            bottom: -20%;
            background: 
                radial-gradient(circle at 15% 15%, rgba(10, 25, 41, 0.08) 0%, transparent 40%),
                radial-gradient(circle at 85% 85%, rgba(201, 169, 110, 0.05) 0%, transparent 40%),
                linear-gradient(135deg, rgba(255, 255, 255, 0.9) 0%, rgba(248, 250, 252, 0.95) 100%);
            pointer-events: none;
            will-change: transform;
            animation: parallaxFloat 20s ease-in-out infinite;
        }

        .hero::after {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background-image: 
                linear-gradient(45deg, transparent 24%, rgba(255, 255, 255, 0.03) 25%, rgba(255, 255, 255, 0.03) 26%, transparent 27%),
                linear-gradient(-45deg, transparent 24%, rgba(10, 25, 41, 0.02) 25%, rgba(10, 25, 41, 0.02) 26%, transparent 27%);
            background-size: 60px 60px;
            pointer-events: none;
            opacity: 0.3;
        }

        @keyframes parallaxFloat {
            0%, 100% { transform: translateY(0px) translateX(0px) rotate(0deg); }
            25% { transform: translateY(-10px) translateX(5px) rotate(0.5deg); }
            50% { transform: translateY(-20px) translateX(0px) rotate(0deg); }
            75% { transform: translateY(-10px) translateX(-5px) rotate(-0.5deg); }
        }

        .hero-content {
            max-width: 900px;
            position: relative;
            z-index: 2;
        }

        .hero h1 {
            font-family: 'Inter', sans-serif;
            font-size: clamp(3rem, 6vw, 5rem);
            font-weight: 200;
            letter-spacing: -0.02em;
            margin-bottom: 2rem;
            background: linear-gradient(135deg, var(--navy-blue) 0%, #c8a882 50%, #d4af37 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            line-height: 1.1;
        }

        .hero p {
            font-size: 1.4rem;
            color: var(--executive-gray);
            margin-bottom: 4rem;
            font-weight: 300;
            line-height: 1.7;
            max-width: 600px;
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            gap: var(--spacing-sm);
            background: linear-gradient(135deg, #c8a882 0%, #d4af37 30%, #e6c66b 60%, #d4af37 85%, #b8860b 100%);
            color: white;
            padding: 1.2rem 2.8rem;
            border: none;
            border-radius: 50px;
            font-size: 1.05rem;
            font-weight: var(--weight-medium);
            font-family: var(--font-primary);
            text-decoration: none;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            overflow: hidden;
            box-shadow: var(--shadow-strong), inset 0 1px 0 rgba(255, 255, 255, 0.2), inset 0 -1px 0 rgba(0, 0, 0, 0.1);
            border: 1px solid rgba(212, 175, 55, 0.3);
            letter-spacing: 0.02em;
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: linear-gradient(135deg, #d4af37 0%, #e6c66b 50%, #c8a882 100%);
            opacity: 0;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            border-radius: 50px;
            z-index: 1;
        }

        .cta-button::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -150%;
            width: 200%;
            height: 200%;
            background: linear-gradient(45deg, transparent 20%, rgba(255, 255, 255, 0.25) 50%, transparent 80%);
            transform: rotate(25deg);
            opacity: 0;
            transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            z-index: 2;
        }

        .cta-button:hover::before {
            opacity: 1;
        }

        .cta-button:hover::after {
            opacity: 1;
            left: 150%;
        }

        .cta-button:hover {
            transform: translateY(-4px);
            box-shadow: var(--shadow-luxury), inset 0 1px 0 rgba(255, 255, 255, 0.3), inset 0 -1px 0 rgba(0, 0, 0, 0.15);
            border-color: rgba(212, 175, 55, 0.5);
        }

        .cta-button:active {
            transform: translateY(-2px);
        }

        .cta-button span {
            position: relative;
            z-index: 3;
        }

        /* Ultra-Luxury Property Showcase */
        .property-showcase {
            padding: var(--spacing-xxl) 0;
            position: relative;
            background: var(--platinum-gradient);
            overflow: hidden;
        }

        .property-showcase::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: 
                radial-gradient(circle at 20% 80%, rgba(10, 25, 41, 0.03) 0%, transparent 50%),
                radial-gradient(circle at 80% 20%, rgba(201, 169, 110, 0.04) 0%, transparent 50%);
            pointer-events: none;
        }

        .property-grid {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr;
            gap: var(--spacing-md);
            max-width: 1400px;
            margin: 0 auto;
        }

        .property-card {
            background: var(--glass-medium);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid var(--glass-strong);
            border-radius: 24px;
            overflow: hidden;
            box-shadow: var(--shadow-medium);
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
        }

        .property-card.featured {
            grid-row: span 1;
        }

        .property-card:hover {
            transform: translateY(-12px);
            box-shadow: var(--shadow-luxury);
            border-color: rgba(201, 169, 110, 0.3);
        }

        .property-image {
            height: 300px;
            position: relative;
            overflow: hidden;
        }

        .property-image .image-placeholder {
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, #c8a882 0%, #d4af37 30%, #e6c66b 60%, #d4af37 85%, #b8860b 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: var(--weight-medium);
            font-size: 1.2rem;
            position: relative;
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
        }

        .property-badge {
            position: absolute;
            top: var(--spacing-sm);
            right: var(--spacing-sm);
            background: linear-gradient(135deg, #c8a882 0%, #d4af37 30%, #e6c66b 60%, #d4af37 85%, #b8860b 100%);
            color: white;
            padding: 0.5rem 1rem;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: var(--weight-semi-bold);
            z-index: 10;
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
            box-shadow: 0 4px 15px rgba(212, 175, 55, 0.3);
        }

        .property-details {
            padding: var(--spacing-md);
        }

        .property-details h3 {
            font-family: var(--font-primary);
            font-size: 1.5rem;
            font-weight: var(--weight-medium);
            color: var(--navy-blue);
            margin-bottom: 0.5rem;
        }

        .property-location {
            color: var(--executive-gray);
            font-size: 1rem;
            margin-bottom: var(--spacing-sm);
        }

        .property-stats {
            display: flex;
            gap: var(--spacing-sm);
            margin-bottom: var(--spacing-sm);
        }

        .property-stats span {
            background: var(--glass-light);
            padding: 0.3rem 0.8rem;
            border-radius: 12px;
            font-size: 0.85rem;
            font-weight: var(--weight-medium);
            color: var(--navy-blue);
        }

        .property-price {
            font-size: 1.8rem;
            font-weight: var(--weight-semi-bold);
            background: linear-gradient(135deg, #c8a882 0%, #d4af37 30%, #e6c66b 60%, #d4af37 85%, #b8860b 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .property-card.featured .property-price {
            font-size: 2.2rem;
        }

        /* Premium Services Section */
        .services {
            padding: var(--spacing-xxl) 0;
            position: relative;
            background: var(--marble-white);
        }

        .services h2 {
            text-align: center;
            font-family: 'Inter', sans-serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 200;
            margin-bottom: 6rem;
            background: linear-gradient(135deg, var(--navy-blue) 0%, #c8a882 50%, #d4af37 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.01em;
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 1.5rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .service-card {
            background: var(--glass-medium);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid var(--glass-strong);
            padding: var(--spacing-md) var(--spacing-sm);
            border-radius: 28px;
            text-align: center;
            box-shadow: var(--shadow-strong);
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            overflow: hidden;
            will-change: transform;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gold-metallic);
            opacity: 0;
            transition: all 0.6s cubic-bezier(0.23, 1, 0.32, 1);
            border-radius: 28px;
            z-index: 1;
        }

        .service-card::after {
            content: '';
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: var(--shimmer-gold);
            opacity: 0;
            transform: rotate(45deg);
            transition: all 0.8s cubic-bezier(0.23, 1, 0.32, 1);
            z-index: 2;
        }

        .service-card:hover::before {
            opacity: 0.08;
        }

        .service-card:hover::after {
            opacity: 0.6;
            transform: rotate(45deg) translateX(100%);
        }

        .service-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-luxury);
            border-color: rgba(201, 169, 110, 0.4);
        }

        .service-card > * {
            position: relative;
            z-index: 3;
        }

        .service-image {
            width: 100%;
            height: 150px;
            margin-bottom: 1.5rem;
            border-radius: 12px;
            overflow: hidden;
            position: relative;
        }

        .service-image img {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .image-placeholder {
            width: 100%;
            height: 100%;
            background: linear-gradient(135deg, var(--navy-blue) 0%, #d4af37 30%, var(--navy-blue) 100%);
            display: flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 300;
            font-size: 0.9rem;
        }

        .service-card h3 {
            font-family: 'Inter', sans-serif;
            font-size: 1.5rem;
            font-weight: 400;
            margin-bottom: 1rem;
            color: var(--navy-blue);
            position: relative;
            z-index: 2;
        }

        .service-card p {
            font-size: 1rem;
            color: var(--executive-gray);
            margin-bottom: 1.5rem;
            line-height: 1.6;
            position: relative;
            z-index: 2;
        }

        .service-price {
            font-size: 1.2rem;
            font-weight: 400;
            background: var(--gold-accent);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 1.5rem;
            position: relative;
            z-index: 2;
        }

        .service-cta {
            background: transparent;
            color: var(--navy-blue);
            border: 2px solid var(--navy-blue);
            padding: 0.8rem 1.8rem;
            border-radius: 25px;
            font-size: 0.9rem;
            font-weight: 400;
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            z-index: 2;
            overflow: hidden;
        }

        .service-cta::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--navy-blue);
            border-radius: 25px;
            transform: scaleX(0);
            transform-origin: left;
            transition: transform 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            z-index: -1;
        }

        .service-cta:hover::before {
            transform: scaleX(1);
        }

        .service-cta:hover {
            color: white;
            transform: translateY(-2px);
        }

        /* Executive Contact Section */
        .contact {
            padding: 10rem 0;
            background: linear-gradient(135deg, #f7fafc 0%, rgba(248, 249, 251, 0.5) 100%);
            position: relative;
        }

        .contact::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: radial-gradient(circle at 70% 80%, rgba(26, 54, 93, 0.05) 0%, transparent 50%);
            pointer-events: none;
        }

        .contact-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 3rem;
            max-width: 1200px;
            margin: 0 auto;
            align-items: stretch;
        }

        .contact h2 {
            text-align: center;
            font-family: 'Inter', sans-serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 200;
            margin-bottom: 6rem;
            background: linear-gradient(135deg, var(--navy-blue) 0%, #c8a882 50%, #d4af37 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.01em;
            grid-column: 1 / -1;
        }

        .map-container {
            background: var(--glass-white);
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            border: 1px solid var(--glass-white-strong);
            border-radius: 20px;
            box-shadow: var(--premium-shadow);
            min-height: 500px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 2rem;
            position: relative;
            z-index: 2;
        }

        .map-icon {
            width: 60px;
            height: 60px;
            background: var(--gold-accent);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 2rem;
            font-size: 1.5rem;
        }

        .map-container h3 {
            font-size: 1.5rem;
            color: var(--navy-blue);
            margin-bottom: 1.5rem;
            font-weight: 300;
        }

        .map-container p {
            color: var(--executive-gray);
            line-height: 1.6;
            margin-bottom: 0.5rem;
        }

        .contact-form {
            background: var(--glass-medium);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid var(--glass-strong);
            padding: var(--spacing-lg);
            border-radius: 24px;
            box-shadow: var(--shadow-strong);
            min-height: 500px;
            position: relative;
            z-index: 2;
        }

        /* Sophisticated Preloader */
        .preloader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--marble-white);
            display: flex;
            align-items: center;
            justify-content: center;
            z-index: 9999;
            transition: opacity 0.8s ease-out, visibility 0.8s ease-out;
        }

        .preloader.fade-out {
            opacity: 0;
            visibility: hidden;
        }

        .loader {
            width: 80px;
            height: 80px;
            position: relative;
        }

        .loader::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: 3px solid transparent;
            border-top: 3px solid var(--gold-accent);
            border-radius: 50%;
            animation: luxurySpinner 1.5s linear infinite;
        }

        .loader::after {
            content: '';
            position: absolute;
            top: 10px;
            left: 10px;
            width: calc(100% - 20px);
            height: calc(100% - 20px);
            border: 2px solid transparent;
            border-bottom: 2px solid var(--navy-blue);
            border-radius: 50%;
            animation: luxurySpinner 1s linear infinite reverse;
        }

        @keyframes luxurySpinner {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .form-group {
            margin-bottom: 1.8rem;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.5rem;
            font-weight: 400;
            color: var(--navy-blue);
            font-size: 0.95rem;
        }

        .form-group input,
        .form-group textarea {
            width: 100%;
            padding: 1.2rem;
            border: 2px solid rgba(26, 54, 93, 0.2);
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            font-family: inherit;
            background: rgba(255, 255, 255, 0.8);
        }

        .form-group input:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--navy-blue);
            transform: translateY(-2px);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.1);
        }

        .form-group textarea {
            height: 120px;
            resize: vertical;
        }

        /* Executive Footer */
        footer {
            background: linear-gradient(135deg, var(--navy-blue) 0%, #2d3748 100%);
            color: white;
            text-align: center;
            padding: 4rem 0;
            position: relative;
        }

        footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gold-accent);
            opacity: 0.05;
        }

        footer p {
            margin-bottom: 1rem;
            font-weight: 300;
            position: relative;
            z-index: 2;
        }

        /* Responsive Excellence */
        @media (max-width: 1024px) {
            .container {
                padding: 0 1.5rem;
            }
            
            .services-grid {
                gap: 1.5rem;
            }
            
            .contact-container {
                grid-template-columns: 1fr;
                gap: 2rem;
            }
        }

        @media (max-width: 768px) {
            .container {
                padding: 0 1rem;
            }

            .services-grid {
                gap: 1rem;
            }

            .service-card {
                padding: 1.8rem 1rem;
            }

            .service-card h3 {
                font-size: 1.4rem;
                margin-bottom: 1rem;
            }

            .service-card p {
                font-size: 0.95rem;
                margin-bottom: 1.5rem;
            }

            .service-price {
                font-size: 1.2rem;
                margin-bottom: 1.5rem;
            }

            .service-cta {
                padding: 0.8rem 1.5rem;
                font-size: 0.9rem;
            }

            .contact-form, .map-container {
                padding: 2rem;
            }

            .floating-square {
                display: none;
            }
        }

        @media (max-width: 480px) {
            .service-card {
                padding: 1.5rem 0.8rem;
            }

            .service-card h3 {
                font-size: 1.2rem;
            }

            .service-card p {
                font-size: 0.9rem;
            }

            .service-cta {
                padding: 0.7rem 1.2rem;
                font-size: 0.85rem;
            }
        }

        /* Performance Optimizations */
        .service-card,
        .cta-button {
            will-change: transform;
        }

        /* Simple Chat Assistant Styles */
        .chat-container {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            z-index: 10000;
            font-family: var(--font-primary);
        }

        .chat-toggle {
            width: 60px;
            height: 60px;
            background: linear-gradient(135deg, var(--navy-blue) 0%, var(--navy-blue-light) 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: var(--shadow-luxury);
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            border: 2px solid rgba(212, 175, 55, 0.2);
            position: relative;
            overflow: hidden;
        }

        .chat-toggle::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gold-accent);
            opacity: 0;
            transition: opacity 0.3s ease;
            border-radius: 50%;
        }

        .chat-toggle:hover::before {
            opacity: 0.1;
        }

        .chat-toggle:hover {
            transform: translateY(-4px) scale(1.05);
            box-shadow: var(--shadow-luxury), 0 0 20px rgba(212, 175, 55, 0.3);
        }

        .chat-icon, .chat-close {
            color: white;
            transition: all 0.3s ease;
            position: relative;
            z-index: 2;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            height: 100%;
        }

        .chat-close {
            display: none;
        }

        .chat-toggle.active .chat-icon {
            display: none;
        }

        .chat-toggle.active .chat-close {
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            height: 100%;
        }

        .chat-widget {
            position: absolute;
            bottom: 80px;
            right: 0;
            width: 350px;
            height: 450px;
            background: var(--glass-medium);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid var(--glass-strong);
            border-radius: 20px;
            box-shadow: var(--shadow-luxury);
            display: flex;
            flex-direction: column;
            transform: translateY(20px) scale(0.95);
            opacity: 0;
            visibility: hidden;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            overflow: hidden;
        }

        .chat-widget.active {
            transform: translateY(0) scale(1);
            opacity: 1;
            visibility: visible;
        }

        .chat-header {
            background: linear-gradient(135deg, var(--navy-blue) 0%, var(--navy-blue-light) 100%);
            padding: 1rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-radius: 20px 20px 0 0;
            position: relative;
        }

        .chat-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--gold-accent);
            opacity: 0.05;
        }

        .agent-info {
            display: flex;
            align-items: center;
            gap: 1rem;
            position: relative;
            z-index: 2;
        }

        .agent-avatar {
            width: 48px;
            height: 48px;
            border-radius: 50%;
            background: var(--gold-accent);
            display: flex;
            align-items: center;
            justify-content: center;
            box-shadow: 0 4px 15px rgba(212, 175, 55, 0.3);
        }

        .avatar-placeholder {
            color: white;
            font-weight: var(--weight-semi-bold);
            font-size: 1.2rem;
            text-shadow: 0 1px 2px rgba(0, 0, 0, 0.2);
        }

        .agent-details h4 {
            color: white;
            font-size: 1.1rem;
            font-weight: var(--weight-medium);
            margin: 0;
        }

        .agent-status {
            color: rgba(255, 255, 255, 0.8);
            font-size: 0.85rem;
            font-weight: var(--weight-normal);
        }

        .chat-minimize {
            background: transparent;
            border: none;
            color: rgba(255, 255, 255, 0.8);
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 8px;
            transition: all 0.3s ease;
            position: relative;
            z-index: 2;
        }

        .chat-minimize:hover {
            background: rgba(255, 255, 255, 0.1);
            color: white;
        }

        .chat-messages {
            flex: 1;
            padding: 1rem;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        .chat-messages::-webkit-scrollbar {
            width: 4px;
        }

        .chat-messages::-webkit-scrollbar-track {
            background: transparent;
        }

        .chat-messages::-webkit-scrollbar-thumb {
            background: rgba(212, 175, 55, 0.3);
            border-radius: 2px;
        }

        .message {
            margin-bottom: 1rem;
            animation: messageSlideIn 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .bot-message .message-content {
            background: linear-gradient(135deg, var(--platinum) 0%, var(--architectural-white) 100%);
            border: 1px solid rgba(226, 232, 240, 0.5);
            padding: 0.8rem 1rem;
            border-radius: 16px 16px 16px 4px;
            max-width: 80%;
            position: relative;
            box-shadow: 0 2px 8px rgba(10, 25, 41, 0.08);
        }

        .user-message {
            text-align: right;
        }

        .user-message .message-content {
            background: linear-gradient(135deg, var(--navy-blue) 0%, var(--navy-blue-light) 100%);
            color: white;
            padding: 0.8rem 1rem;
            border-radius: 16px 16px 4px 16px;
            max-width: 80%;
            margin-left: auto;
            display: inline-block;
            box-shadow: 0 2px 8px rgba(26, 54, 93, 0.2);
        }

        .message-content p {
            margin: 0;
            line-height: 1.5;
            font-size: 0.95rem;
        }

        .message-time {
            font-size: 0.75rem;
            color: var(--executive-gray);
            margin-top: 0.5rem;
            opacity: 0.7;
        }

        .quick-actions {
            padding: 1rem 1.5rem;
            border-top: 1px solid var(--glass-medium);
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 0.75rem;
        }

        .quick-action-btn {
            background: var(--glass-light);
            border: 1px solid var(--glass-medium);
            padding: 0.75rem;
            border-radius: 12px;
            font-size: 0.85rem;
            font-weight: var(--weight-medium);
            color: var(--navy-blue);
            cursor: pointer;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            display: flex;
            align-items: center;
            gap: 0.5rem;
            text-align: left;
        }

        .quick-action-btn:hover {
            background: var(--glass-medium);
            border-color: rgba(212, 175, 55, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        }

        .action-icon {
            font-size: 1rem;
        }

        .chat-input-container {
            padding: 0.8rem 1rem;
            border-top: 1px solid var(--glass-medium);
        }

        .chat-input {
            display: flex;
            gap: 0.75rem;
            align-items: center;
        }

        .chat-input input {
            flex: 1;
            padding: 1rem;
            border: 2px solid var(--glass-medium);
            border-radius: 12px;
            font-size: 0.95rem;
            background: var(--glass-light);
            transition: all 0.3s ease;
            font-family: inherit;
        }

        .chat-input input:focus {
            outline: none;
            border-color: rgba(212, 175, 55, 0.5);
            background: rgba(255, 255, 255, 0.9);
        }

        .send-button {
            width: 44px;
            height: 44px;
            background: linear-gradient(135deg, var(--navy-blue) 0%, var(--navy-blue-light) 100%);
            border: none;
            border-radius: 12px;
            color: white;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .send-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(26, 54, 93, 0.3);
        }

        .typing-indicator {
            display: none;
            padding: 0 1rem 0.8rem;
            align-items: center;
            gap: 0.75rem;
            font-size: 0.85rem;
            color: var(--executive-gray);
        }

        .typing-indicator.active {
            display: flex;
        }

        .typing-dots {
            display: flex;
            gap: 0.25rem;
        }

        .typing-dots .dot {
            width: 6px;
            height: 6px;
            background: var(--navy-blue);
            border-radius: 50%;
            animation: typingBounce 1.4s infinite ease-in-out;
        }

        .typing-dots .dot:nth-child(1) { animation-delay: -0.32s; }
        .typing-dots .dot:nth-child(2) { animation-delay: -0.16s; }

        .lead-capture-form {
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--marble-white);
            border-radius: 24px;
            padding: 2rem;
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
        }

        .form-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .form-header h4 {
            color: var(--navy-blue);
            font-size: 1.3rem;
            font-weight: var(--weight-medium);
            margin-bottom: 0.5rem;
        }

        .form-header p {
            color: var(--executive-gray);
            font-size: 0.9rem;
            line-height: 1.5;
        }

        .lead-capture-form .form-group {
            margin-bottom: 1.2rem;
        }

        .lead-capture-form input,
        .lead-capture-form select {
            width: 100%;
            padding: 1rem;
            border: 2px solid rgba(26, 54, 93, 0.2);
            border-radius: 12px;
            font-size: 0.95rem;
            font-family: inherit;
            background: rgba(255, 255, 255, 0.8);
            transition: all 0.3s ease;
        }

        .lead-capture-form input:focus,
        .lead-capture-form select:focus {
            outline: none;
            border-color: var(--navy-blue);
            background: white;
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        }

        .form-actions {
            display: flex;
            gap: 1rem;
            margin-top: 1.5rem;
        }

        .form-cancel {
            flex: 1;
            padding: 1rem;
            background: transparent;
            border: 2px solid var(--glass-medium);
            border-radius: 12px;
            color: var(--executive-gray);
            font-weight: var(--weight-medium);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .form-cancel:hover {
            background: var(--glass-light);
        }

        .form-submit {
            flex: 2;
            padding: 1rem;
            background: linear-gradient(135deg, var(--navy-blue) 0%, var(--navy-blue-light) 100%);
            border: none;
            border-radius: 12px;
            color: white;
            font-weight: var(--weight-medium);
            cursor: pointer;
            transition: all 0.3s ease;
        }

        .form-submit:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(26, 54, 93, 0.3);
        }

        @keyframes messageSlideIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes typingBounce {
            0%, 80%, 100% {
                transform: scale(0);
                opacity: 0.5;
            }
            40% {
                transform: scale(1);
                opacity: 1;
            }
        }

        /* Mobile Responsive */
        @media (max-width: 768px) {
            .chat-container {
                bottom: 1rem;
                right: 1rem;
            }

            .chat-widget {
                width: calc(100vw - 2rem);
                height: 400px;
                right: -1rem;
            }
        }

        /* Accessibility */
        @media (prefers-reduced-motion: reduce) {
            *,
            *::before,
            *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
            
            .floating-square {
                animation: none;
            }
        }
    </style>
</head>
<body>
    <!-- Sophisticated Preloader -->
    <div class="preloader" id="preloader">
        <div class="loader"></div>
    </div>

    <!-- Architectural Blueprint Elements -->
    <div class="floating-elements">
        <div class="floating-square"></div>
        <div class="floating-square"></div>
        <div class="floating-square"></div>
        <div class="floating-square"></div>
        <div class="floating-square"></div>
        <div class="floating-square"></div>
    </div>

    <!-- Executive Header -->
    <header id="header">
        <div class="container">
            <div class="header-content">
                <div class="logo">{{businessName}}</div>
                <nav>
                    <ul>
                        <li><a href="#home">Home</a></li>
                        <li><a href="#properties">Portfolio</a></li>
                        <li><a href="#services">Services</a></li>
                        <li><a href="#contact">Contact</a></li>
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <!-- Executive Hero Section -->
    <section class="hero reveal-element" id="home">
        <div class="container">
            <div class="hero-content">
                <h1>{{heroTitle}}</h1>
                <p>{{tagline}}</p>
                <a href="#properties" class="cta-button">
                    <span>Schedule Consultation</span>
                </a>
            </div>
        </div>
    </section>

    <!-- Luxury Property Showcase -->
    <section class="property-showcase" id="properties">
        <div class="container">
            <h2 class="reveal-element">Featured Properties</h2>
            <div class="property-grid reveal-element">
                <div class="property-card featured">
                    <div class="property-image">
                        <div class="image-placeholder">\$12.5M Estate</div>
                        <div class="property-badge">Featured</div>
                    </div>
                    <div class="property-details">
                        <h3>Beverly Hills Estate</h3>
                        <p class="property-location">Beverly Hills, CA</p>
                        <div class="property-stats">
                            <span>8 BD</span>
                            <span>10 BA</span>
                            <span>15,000 sqft</span>
                        </div>
                        <div class="property-price">\$12,500,000</div>
                    </div>
                </div>
                
                <div class="property-card">
                    <div class="property-image">
                        <div class="image-placeholder">\$8.2M Penthouse</div>
                    </div>
                    <div class="property-details">
                        <h3>Manhattan Penthouse</h3>
                        <p class="property-location">Upper East Side, NY</p>
                        <div class="property-stats">
                            <span>4 BD</span>
                            <span>5 BA</span>
                            <span>4,500 sqft</span>
                        </div>
                        <div class="property-price">\$8,200,000</div>
                    </div>
                </div>
                
                <div class="property-card">
                    <div class="property-image">
                        <div class="image-placeholder">\$6.8M Villa</div>
                    </div>
                    <div class="property-details">
                        <h3>Malibu Ocean Villa</h3>
                        <p class="property-location">Malibu, CA</p>
                        <div class="property-stats">
                            <span>6 BD</span>
                            <span>7 BA</span>
                            <span>8,200 sqft</span>
                        </div>
                        <div class="property-price">\$6,800,000</div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Premium Services Section -->
    <section class="services" id="services">
        <div class="container">
            <h2 class="reveal-element">Executive Services</h2>
            <div class="services-grid reveal-element">
                <div class="service-card">
                    <div class="service-image">
                        <div class="image-placeholder">Market Analysis</div>
                    </div>
                    <h3>Market Intelligence</h3>
                    <p>Comprehensive property valuation and market insights to maximize your investment potential and strategic positioning in luxury markets</p>
                    <div class="service-price">Complimentary</div>
                    <button class="service-cta">Get Analysis</button>
                </div>
                
                <div class="service-card">
                    <div class="service-image">
                        <div class="image-placeholder">Property Marketing</div>
                    </div>
                    <h3>Luxury Marketing</h3>
                    <p>Premium listing presentation and targeted buyer attraction strategies with exclusive market positioning and global reach</p>
                    <div class="service-price">Commission Based</div>
                    <button class="service-cta">Learn More</button>
                </div>
                
                <div class="service-card">
                    <div class="service-image">
                        <div class="image-placeholder">Closing Support</div>
                    </div>
                    <h3>White-Glove Service</h3>
                    <p>Expert transaction management and client advocacy throughout the entire luxury property acquisition and closing process</p>
                    <div class="service-price">Full Service</div>
                    <button class="service-cta">Get Started</button>
                </div>
            </div>
        </div>
    </section>

    <!-- Executive Contact Section -->
    <section class="contact" id="contact">
        <div class="container">
            <h2 class="reveal-element">Schedule Your Consultation</h2>
            <div class="contact-container reveal-element">
                <div class="map-container">
                    <div class="map-icon">📍</div>
                    <h3>Visit Our Office</h3>
                    <p>{{businessAddress}}</p>
                    <p>{{businessPhone}}</p>
                    <p>{{mapEmbedCode}}</p>
                </div>
                
                <form class="contact-form">
                    <div class="form-group">
                        <label for="name">Full Name</label>
                        <input type="text" id="name" name="name" required>
                    </div>
                    <div class="form-group">
                        <label for="email">Email Address</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="phone">Phone Number</label>
                        <input type="tel" id="phone" name="phone">
                    </div>
                    <div class="form-group">
                        <label for="message">Property Interests</label>
                        <textarea id="message" name="message" placeholder="Tell us about your property goals..."></textarea>
                    </div>
                    <button type="submit" class="cta-button">
                        <span>Schedule Consultation</span>
                    </button>
                </form>
            </div>
        </div>
    </section>

    <!-- Executive Footer -->
    <footer>
        <div class="container">
            <p>&copy; 2024 {{businessName}}. All rights reserved.</p>
            <p>{{businessAddress}} | {{businessPhone}}</p>
        </div>
    </footer>

    <!-- Simple Chat Assistant -->
    <div class="chat-container" id="chatContainer">
        <!-- Chat Toggle Button -->
        <div class="chat-toggle" id="chatToggle">
            <div class="chat-icon">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M20 2H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h4l4 4 4-4h4c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm-2 12H6v-2h12v2zm0-3H6V9h12v2zm0-3H6V6h12v2z"/>
                </svg>
            </div>
            <div class="chat-close" id="chatClose">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/>
                </svg>
            </div>
        </div>

        <!-- Chat Widget -->
        <div class="chat-widget" id="chatWidget">
            <!-- Chat Header -->
            <div class="chat-header">
                <div class="agent-info">
                    <div class="agent-details">
                        <h4>Real Estate Assistant</h4>
                        <span class="agent-status">Ask me anything</span>
                    </div>
                </div>
                <button class="chat-minimize" id="chatMinimize">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M19 13H5v-2h14v2z"/>
                    </svg>
                </button>
            </div>

            <!-- Chat Messages -->
            <div class="chat-messages" id="chatMessages">
                <div class="message bot-message">
                    <div class="message-content">
                        <p>Hello! I'm here to answer questions about our real estate services. What would you like to know?</p>
                    </div>
                    <div class="message-time">Just now</div>
                </div>
            </div>

            <!-- Chat Input -->
            <div class="chat-input-container">
                <div class="chat-input">
                    <input type="text" id="messageInput" placeholder="Ask a question...">
                    <button class="send-button" id="sendButton">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/>
                        </svg>
                    </button>
                </div>
            </div>

            <!-- Typing Indicator -->
            <div class="typing-indicator" id="typingIndicator">
                <div class="typing-dots">
                    <div class="dot"></div>
                    <div class="dot"></div>
                    <div class="dot"></div>
                </div>
                <span>Typing...</span>
            </div>
        </div>
    </div>

    <!-- Ultra-Executive JavaScript -->
    <script>
        // Sophisticated Preloader
        window.addEventListener('load', () => {
            const preloader = document.getElementById('preloader');
            setTimeout(() => {
                preloader.classList.add('fade-out');
                setTimeout(() => {
                    preloader.style.display = 'none';
                }, 800);
            }, 1200);
        });

        // Executive Header Scroll Effect with Parallax
        let ticking = false;
        function updateHeader() {
            const header = document.getElementById('header');
            const scrolled = window.pageYOffset;
            
            if (scrolled > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
            
            // Parallax effect for hero background
            const heroBackground = document.querySelector('.hero::before');
            if (scrolled < window.innerHeight) {
                const parallaxOffset = scrolled * 0.5;
                if (heroBackground) {
                    document.querySelector('.hero').style.transform = \`translateY(\${parallaxOffset}px)\`;
                }
            }
            
            ticking = false;
        }

        window.addEventListener('scroll', () => {
            if (!ticking) {
                requestAnimationFrame(updateHeader);
                ticking = true;
            }
        });

        // Advanced Intersection Observer for Reveal Animations
        const revealElements = document.querySelectorAll('.reveal-element');
        const revealObserver = new IntersectionObserver((entries) => {
            entries.forEach((entry, index) => {
                if (entry.isIntersecting) {
                    setTimeout(() => {
                        entry.target.classList.add('revealed');
                    }, index * 100); // Staggered animation
                }
            });
        }, {
            threshold: 0.15,
            rootMargin: '0px 0px -80px 0px'
        });

        revealElements.forEach(element => {
            revealObserver.observe(element);
        });

        // Luxury Smooth Scroll with Momentum
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const targetId = this.getAttribute('href');
                const target = document.querySelector(targetId);
                
                if (target) {
                    const headerHeight = document.querySelector('header').offsetHeight;
                    const targetPosition = target.offsetTop - headerHeight - 20;
                    
                    window.scrollTo({
                        top: targetPosition,
                        behavior: 'smooth'
                    });
                }
            });
        });

        // Executive Form Interactions with Floating Labels
        const formInputs = document.querySelectorAll('input, textarea');
        formInputs.forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.style.transform = 'translateY(-4px)';
                this.parentElement.style.transition = 'all 0.4s cubic-bezier(0.23, 1, 0.32, 1)';
            });
            
            input.addEventListener('blur', function() {
                this.parentElement.style.transform = 'translateY(0)';
            });

            // Sophisticated validation states
            input.addEventListener('input', function() {
                if (this.value.length > 0) {
                    this.style.borderColor = 'rgba(201, 169, 110, 0.5)';
                } else {
                    this.style.borderColor = 'rgba(10, 25, 41, 0.2)';
                }
            });
        });

        // Simple Elegant Card Hover Effects
        const serviceCards = document.querySelectorAll('.service-card, .property-card');
        serviceCards.forEach(card => {
            card.addEventListener('mouseenter', function() {
                this.style.willChange = 'transform';
            });
            
            card.addEventListener('mouseleave', function() {
                this.style.willChange = 'auto';
            });
        });

        // Executive CTA Button Ripple Effect
        const ctaButtons = document.querySelectorAll('.cta-button, .service-cta');
        ctaButtons.forEach(button => {
            button.addEventListener('click', function(e) {
                const ripple = document.createElement('div');
                const rect = this.getBoundingClientRect();
                const size = Math.max(rect.width, rect.height);
                const x = e.clientX - rect.left - size / 2;
                const y = e.clientY - rect.top - size / 2;
                
                ripple.style.width = ripple.style.height = size + 'px';
                ripple.style.left = x + 'px';
                ripple.style.top = y + 'px';
                ripple.style.position = 'absolute';
                ripple.style.borderRadius = '50%';
                ripple.style.background = 'rgba(255, 255, 255, 0.6)';
                ripple.style.transform = 'scale(0)';
                ripple.style.animation = 'ripple 0.6s linear';
                ripple.style.pointerEvents = 'none';
                
                this.appendChild(ripple);
                
                setTimeout(() => {
                    ripple.remove();
                }, 600);
            });
        });

        // Sophisticated Error States for Forms
        const form = document.querySelector('.contact-form');
        if (form) {
            form.addEventListener('submit', function(e) {
                e.preventDefault();
                
                // Advanced form validation with luxury styling
                const inputs = this.querySelectorAll('input[required], textarea[required]');
                let isValid = true;
                
                inputs.forEach(input => {
                    if (!input.value.trim()) {
                        isValid = false;
                        input.style.borderColor = '#ef4444';
                        input.style.boxShadow = '0 0 0 3px rgba(239, 68, 68, 0.1)';
                    } else {
                        input.style.borderColor = 'rgba(201, 169, 110, 0.5)';
                        input.style.boxShadow = '0 0 0 3px rgba(201, 169, 110, 0.1)';
                    }
                });
                
                if (isValid) {
                    // Luxury success animation
                    const submitBtn = this.querySelector('[type="submit"]');
                    submitBtn.textContent = 'Sent Successfully ✓';
                    submitBtn.style.background = 'linear-gradient(135deg, #10b981 0%, #059669 100%)';
                }
            });
        }

        // Premium Performance Optimization
        if ('requestIdleCallback' in window) {
            requestIdleCallback(() => {
                // Preload critical resources
                const criticalImages = document.querySelectorAll('img[data-src]');
                const imageObserver = new IntersectionObserver((entries) => {
                    entries.forEach(entry => {
                        if (entry.isIntersecting) {
                            const img = entry.target;
                            img.src = img.dataset.src;
                            img.removeAttribute('data-src');
                            imageObserver.unobserve(img);
                        }
                    });
                });
                
                criticalImages.forEach(img => imageObserver.observe(img));
                
                console.log('Ultra-luxury Sotheby\'s International experience ready');
            });
        }

        // CSS Animation for ripple effect
        const style = document.createElement('style');
        style.textContent = \`
            @keyframes ripple {
                to {
                    transform: scale(4);
                    opacity: 0;
                }
            }
        \`;
        document.head.appendChild(style);

        // Simple Real Estate Chat Assistant
        class SimpleChatAssistant {
            constructor() {
                this.isOpen = false;
                this.responses = {
                    welcome: "Hello! I'm here to answer questions about our real estate services. What would you like to know?",
                    
                    // Simple Q&A responses
                    hours: "Our office hours are Monday-Friday 9AM-6PM, Saturday 10AM-4PM. You can also call us anytime at {{businessPhone}}.",
                    location: "We're located at {{businessAddress}}. Feel free to stop by during business hours!",
                    services: "We offer buying assistance, selling services, market analysis, property management, and investment consulting. How can we help you?",
                    buying: "I can help you find the perfect property! We work with all price ranges and have access to exclusive listings. What's your ideal price range?",
                    selling: "We provide full-service selling including professional photography, marketing, and expert negotiation. What type of property are you looking to sell?",
                    market: "I can provide a free market analysis for your property. This includes recent comparable sales and current market trends in your area.",
                    contact: "You can reach us at {{businessPhone}} or email us. We typically respond within a few hours during business hours.",
                    commission: "Our commission rates are competitive and include full marketing, professional photos, and expert support through closing.",
                    
                    default: "That's a great question! For detailed information, please call us at {{businessPhone}} or visit our office. We'd be happy to help!"
                };
                
                this.init();
            }
            
            init() {
                this.bindEvents();
            }
            
            bindEvents() {
                const chatToggle = document.getElementById('chatToggle');
                const chatMinimize = document.getElementById('chatMinimize');
                const sendButton = document.getElementById('sendButton');
                const messageInput = document.getElementById('messageInput');
                
                chatToggle.addEventListener('click', () => this.toggleChat());
                chatMinimize.addEventListener('click', () => this.closeChat());
                sendButton.addEventListener('click', () => this.sendMessage());
                messageInput.addEventListener('keypress', (e) => {
                    if (e.key === 'Enter') this.sendMessage();
                });
            }
            
            toggleChat() {
                const chatWidget = document.getElementById('chatWidget');
                const chatToggle = document.getElementById('chatToggle');
                
                if (this.isOpen) {
                    this.closeChat();
                } else {
                    this.openChat();
                }
            }
            
            openChat() {
                const chatWidget = document.getElementById('chatWidget');
                const chatToggle = document.getElementById('chatToggle');
                
                chatWidget.classList.add('active');
                chatToggle.classList.add('active');
                this.isOpen = true;
                
                // Focus on input
                setTimeout(() => {
                    document.getElementById('messageInput').focus();
                }, 400);
            }
            
            closeChat() {
                const chatWidget = document.getElementById('chatWidget');
                const chatToggle = document.getElementById('chatToggle');
                
                chatWidget.classList.remove('active');
                chatToggle.classList.remove('active');
                this.isOpen = false;
            }
            
            addMessage(content, isUser = false, delay = 0) {
                setTimeout(() => {
                    const messagesContainer = document.getElementById('chatMessages');
                    const messageDiv = document.createElement('div');
                    messageDiv.className = \`message \${isUser ? 'user-message' : 'bot-message'}\`;
                    
                    const now = new Date();
                    const timeString = now.toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'});
                    
                    messageDiv.innerHTML = \`
                        <div class="message-content">
                            <p>\${content}</p>
                        </div>
                        <div class="message-time">\${timeString}</div>
                    \`;
                    
                    messagesContainer.appendChild(messageDiv);
                    messagesContainer.scrollTop = messagesContainer.scrollHeight;
                }, delay);
            }
            
            showTyping() {
                const typingIndicator = document.getElementById('typingIndicator');
                typingIndicator.classList.add('active');
            }
            
            hideTyping() {
                const typingIndicator = document.getElementById('typingIndicator');
                typingIndicator.classList.remove('active');
            }
            
            sendMessage() {
                const messageInput = document.getElementById('messageInput');
                const message = messageInput.value.trim();
                
                if (!message) return;
                
                this.addMessage(message, true);
                messageInput.value = '';
                
                this.showTyping();
                setTimeout(() => {
                    this.hideTyping();
                    this.processMessage(message);
                }, 1500);
            }
            
            processMessage(message) {
                const lowerMessage = message.toLowerCase();
                let response = '';
                
                // Simple keyword detection for basic Q&A
                if (lowerMessage.includes('hours') || lowerMessage.includes('open') || lowerMessage.includes('time')) {
                    response = this.responses.hours;
                } else if (lowerMessage.includes('location') || lowerMessage.includes('address') || lowerMessage.includes('where')) {
                    response = this.responses.location;
                } else if (lowerMessage.includes('services') || lowerMessage.includes('what do you') || lowerMessage.includes('help')) {
                    response = this.responses.services;
                } else if (lowerMessage.includes('buy') || lowerMessage.includes('buying') || lowerMessage.includes('purchase')) {
                    response = this.responses.buying;
                } else if (lowerMessage.includes('sell') || lowerMessage.includes('selling') || lowerMessage.includes('listing')) {
                    response = this.responses.selling;
                } else if (lowerMessage.includes('market') || lowerMessage.includes('value') || lowerMessage.includes('analysis')) {
                    response = this.responses.market;
                } else if (lowerMessage.includes('contact') || lowerMessage.includes('phone') || lowerMessage.includes('call')) {
                    response = this.responses.contact;
                } else if (lowerMessage.includes('commission') || lowerMessage.includes('fee') || lowerMessage.includes('cost')) {
                    response = this.responses.commission;
                } else if (lowerMessage.includes('hello') || lowerMessage.includes('hi') || lowerMessage.includes('hey')) {
                    response = this.responses.welcome;
                } else {
                    response = this.responses.default;
                }
                
                this.addMessage(response);
            }
            

        }
        
        // Initialize the simple chat assistant when page loads
        window.addEventListener('DOMContentLoaded', () => {
            new SimpleChatAssistant();
        });
    </script>
</body>
</html>`,
    
    // HVAC TEMPLATE - COMPLETE WITH ALL SECTIONS
    'hvac': `<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{businessName}} | Premium HVAC Services | {{heroTitle}}</title>
    <style>
        @import url('https://fonts.googleapis.com/css2?family=Inter:wght@100;200;300;400;500;600;700;800;900&display=swap');
        @import url('https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@300;400;500;600;700;800&display=swap');
        
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-color: {{primaryColor}};
            --secondary-color: {{secondaryColor}};
            --background-color: {{backgroundColor}};
            --text-color: {{textColor}};
            --accent-color: {{accentColor}};
            
            /* Color-polish variables */
            --brand-red: #D91E18;
            --neutral-dark: rgba(0,0,0,0.45);
            
            /* Premium HVAC Professional Palette */
            --trust-blue: #0f172a;
            --emergency-red: #dc2626;
            --emergency-orange: #ea580c;
            --professional-navy: #1e293b;
            --reliable-gray: #475569;
            --premium-white: #ffffff;
            --success-green: #059669;
            --warning-amber: #f59e0b;
            
            /* Advanced Gradients */
            --emergency-gradient: linear-gradient(135deg, #dc2626 0%, #b91c1c 50%, #991b1b 100%);
            --trust-gradient: linear-gradient(135deg, #0f172a 0%, #1e293b 50%, #334155 100%);
            --professional-gradient: linear-gradient(135deg, #64748b 0%, #475569 50%, #334155 100%);
            --success-gradient: linear-gradient(135deg, #059669 0%, #047857 50%, #065f46 100%);
            
            /* Glass Effects */
            --glass-white: rgba(255, 255, 255, 0.08);
            --glass-white-strong: rgba(255, 255, 255, 0.15);
            --glass-dark: rgba(15, 23, 42, 0.08);
            --glass-emergency: rgba(220, 38, 38, 0.1);
            
            /* Shadows & Effects */
            --shadow-premium: 0 25px 50px rgba(15, 23, 42, 0.08);
            --shadow-hover: 0 35px 70px rgba(15, 23, 42, 0.12);
            --shadow-emergency: 0 20px 40px rgba(220, 38, 38, 0.2);
            --shadow-floating: 0 10px 25px rgba(15, 23, 42, 0.1);
            
            /* Professional Textures */
            --metal-texture: linear-gradient(45deg, rgba(255,255,255,0.1) 25%, transparent 25%), 
                             linear-gradient(-45deg, rgba(255,255,255,0.1) 25%, transparent 25%);
            --blueprint-pattern: radial-gradient(circle at 2px 2px, rgba(30, 64, 175, 0.1) 1px, transparent 0);
            --shimmer: linear-gradient(45deg, transparent 30%, rgba(255, 255, 255, 0.4) 50%, transparent 70%);
        }

        html {
            scroll-behavior: smooth;
            font-size: 16px;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, system-ui, sans-serif;
            line-height: 1.6;
            color: var(--professional-navy);
            background: var(--premium-white);
            overflow-x: hidden;
            position: relative;
        }

        /* Premium Preloader */
        .premium-preloader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: var(--trust-gradient);
            z-index: 10000;
            display: flex;
            align-items: center;
            justify-content: center;
            flex-direction: column;
            color: white;
            transition: opacity 0.5s ease, visibility 0.5s ease;
        }

        .preloader-content {
            text-align: center;
        }

        .hvac-spinner {
            width: 80px;
            height: 80px;
            border: 4px solid rgba(255, 255, 255, 0.1);
            border-top: 4px solid var(--emergency-orange);
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-bottom: 2rem;
        }

        @keyframes spin {
            0% { transform: rotate(0deg); }
            100% { transform: rotate(360deg); }
        }

        .preloader-text {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.2rem;
            font-weight: 500;
            letter-spacing: 0.05em;
        }





        /* Floating Background Elements */
        .floating-elements {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            pointer-events: none;
            z-index: -1;
            overflow: hidden;
        }

        .floating-blueprint {
            position: absolute;
            background: var(--blueprint-pattern);
            background-size: 20px 20px;
            border-radius: 12px;
            opacity: 0.03;
            animation: float 25s infinite ease-in-out;
        }

        .floating-blueprint:nth-child(1) {
            width: 300px;
            height: 200px;
            top: 10%;
            left: 5%;
            animation-delay: 0s;
        }

        .floating-blueprint:nth-child(2) {
            width: 250px;
            height: 150px;
            top: 60%;
            right: 8%;
            animation-delay: -8s;
        }

        .floating-blueprint:nth-child(3) {
            width: 200px;
            height: 250px;
            bottom: 20%;
            left: 15%;
            animation-delay: -16s;
        }

        .floating-blueprint:nth-child(4) {
            width: 350px;
            height: 180px;
            top: 35%;
            right: 20%;
            animation-delay: -12s;
        }

        @keyframes float {
            0%, 100% { transform: translateY(0px) rotate(0deg); }
            25% { transform: translateY(-15px) rotate(1deg); }
            50% { transform: translateY(-30px) rotate(-1deg); }
            75% { transform: translateY(-15px) rotate(2deg); }
        }

        .container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Reveal Animations */
        .reveal-element {
            opacity: 0;
            transform: translateY(60px);
            transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .reveal-element.revealed {
            opacity: 1;
            transform: translateY(0);
        }

        .reveal-left {
            opacity: 0;
            transform: translateX(-60px);
            transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .reveal-left.revealed {
            opacity: 1;
            transform: translateX(0);
        }

        .reveal-right {
            opacity: 0;
            transform: translateX(60px);
            transition: all 0.8s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .reveal-right.revealed {
            opacity: 1;
            transform: translateX(0);
        }

        /* Premium Header */
        header {
            background: rgba(255, 255, 255, 0.95);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border-bottom: 1px solid rgba(15, 23, 42, 0.08);
            padding: 1.5rem 0;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        header.scrolled {
            background: rgba(255, 255, 255, 0.98);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            box-shadow: var(--shadow-premium);
            padding: 1rem 0;
        }

        .header-content {
            display: flex;
            justify-content: space-between;
            align-items: center;
            flex-wrap: wrap;
            gap: 1rem;
        }

        .logo {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.8rem;
            font-weight: 700;
            background: var(--trust-gradient);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.02em;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .logo::before {
            content: '⚙';
            font-size: 1.5rem;
        }



        nav ul {
            display: flex;
            list-style: none;
            gap: 2.5rem;
        }

        nav a {
            text-decoration: none;
            color: var(--professional-navy);
            font-weight: 500;
            font-size: 1rem;
            position: relative;
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            padding: 0.5rem 0;
        }

        nav a::after {
            content: '';
            position: absolute;
            bottom: 0;
            left: 0;
            width: 0;
            height: 3px;
            background: var(--emergency-gradient);
            transition: width 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            border-radius: 2px;
        }

        nav a:hover::after {
            width: 100%;
        }

        nav a:hover {
            color: var(--emergency-red);
            transform: translateY(-2px);
        }



        /* Hero Section with Background */
        .hero {
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding-top: 120px;
            position: relative;
            overflow: hidden;
        }

        /* Background Image with Blur */
        .hero::after {
            content: "";
            position: absolute;
            inset: -20px;
            background: url('hvac acc/hvachero.png') center/cover no-repeat;
            background-position: 40% 50%;
            filter: blur(2px);
            z-index: -3;
        }

        /* Dark Base Layer */
        .hero::before {
            content: "";
            position: absolute;
            inset: 0;
            background: rgba(0, 0, 0, 0.4);
            z-index: -2;
        }

        /* Gradient Overlay */
        .hero-overlay {
            position: absolute;
            inset: 0;
            background: linear-gradient(
                to bottom,
                #ffffff 0%,
                rgba(255,255,255,0.8) 10%,
                rgba(255,255,255,0) 20%,
                rgba(0,0,0,0.3) 60%,
                rgba(0,0,0,0.7) 85%,
                rgba(0,0,0,0.8) 100%
            );
            pointer-events: none;
            z-index: -1;
        }

        /* Keep hero content above overlay */
        .hero > * {
            position: relative;
            z-index: 1;
        }

        .hero-content {
            max-width: 1000px;
            position: relative;
            z-index: 1;
        }

        .hero-badges {
            display: flex;
            gap: 1rem;
            margin-bottom: 2rem;
            flex-wrap: wrap;
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            background: rgba(0, 0, 0, 0.5);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            color: white;
            padding: 0.6rem 1.2rem;
            border-radius: 9999px;
            font-size: 0.9rem;
            font-weight: 500;
            transition: all 0.3s ease;
            border: 1px solid rgba(255, 255, 255, 0.2);
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.3);
        }

        .hero-badge:hover {
            background: rgba(255, 255, 255, 0.3);
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.2);
        }

        .emergency-badge {
            background: linear-gradient(135deg, #dc2626, #c53030) !important;
            backdrop-filter: blur(20px);
            -webkit-backdrop-filter: blur(20px);
            color: white !important;
            border: 1px solid #dc2626 !important;
            animation: emergencyPulse 2s infinite;
            box-shadow: 
                0 0 20px rgba(220, 38, 38, 0.4),
                0 4px 6px rgba(15, 23, 42, 0.2),
                inset 0 1px 0 rgba(255, 255, 255, 0.2);
        }

        @keyframes emergencyPulse {
            0%, 100% { 
                box-shadow: 
                    0 0 20px rgba(220, 38, 38, 0.4),
                    0 4px 6px rgba(15, 23, 42, 0.2),
                    inset 0 1px 0 rgba(255, 255, 255, 0.2);
                transform: scale(1);
            }
            50% { 
                box-shadow: 
                    0 0 35px rgba(220, 38, 38, 0.6),
                    0 0 50px rgba(220, 38, 38, 0.3),
                    0 6px 8px rgba(15, 23, 42, 0.25),
                    inset 0 1px 0 rgba(255, 255, 255, 0.3);
                transform: scale(1.02);
            }
        }

        .hero h1 {
            font-family: 'Space Grotesk', sans-serif;
            font-size: clamp(3.5rem, 8vw, 6rem);
            font-weight: 800;
            letter-spacing: -0.03em;
            margin-bottom: 2rem;
            color: white;
            line-height: 1.1;
            position: relative;
            text-shadow: 
                0 2px 4px rgba(0, 0, 0, 0.3),
                0 4px 8px rgba(0, 0, 0, 0.4),
                0 8px 16px rgba(0, 0, 0, 0.3);
        }

        .hero p {
            font-size: 1.4rem;
            color: rgba(255, 255, 255, 0.95);
            margin-bottom: 3rem;
            font-weight: 400;
            line-height: 1.7;
            max-width: 650px;
            text-shadow: 
                0 1px 3px rgba(0, 0, 0, 0.5),
                0 2px 6px rgba(0, 0, 0, 0.3);
        }

        .hero-stats {
            display: flex;
            gap: 3rem;
            margin-bottom: 4rem;
            flex-wrap: wrap;
        }

        .stat-item {
            text-align: center;
        }

        .stat-number {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 2.5rem;
            font-weight: 800;
            color: white;
            display: block;
            line-height: 1;
            text-shadow: 
                0 2px 4px rgba(0, 0, 0, 0.4),
                0 4px 8px rgba(0, 0, 0, 0.3);
        }

        .stat-label {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 500;
            margin-top: 10px;
            text-shadow: 0 1px 3px rgba(0, 0, 0, 0.5);
        }

        .hero-ctas {
            display: flex;
            gap: 1.5rem;
            flex-wrap: wrap;
        }

        .cta-button {
            display: inline-flex;
            align-items: center;
            gap: 1rem;
            background: linear-gradient(135deg, #1e40af, #1e3a8a);
            color: white;
            padding: 1.5rem 3rem;
            border: none;
            border-radius: 50px;
            font-size: 1.1rem;
            font-weight: 700;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            overflow: hidden;
            box-shadow: 0 10px 25px rgba(30, 64, 175, 0.3);
            font-family: 'Inter', sans-serif;
        }

        .cta-button::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--shimmer);
            transition: left 0.6s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .cta-button:hover::before {
            left: 100%;
        }

        .cta-button:hover {
            transform: translateY(-5px);
            box-shadow: 0 35px 70px rgba(30, 64, 175, 0.4);
            background: linear-gradient(135deg, #1d4ed8, #1e40af);
        }

        .cta-secondary {
            background: rgba(255, 255, 255, 0.15);
            backdrop-filter: blur(10px);
            color: white;
            border: 3px solid rgba(255, 255, 255, 0.4);
            box-shadow: 
                0 4px 6px rgba(0, 0, 0, 0.3),
                0 10px 25px rgba(0, 0, 0, 0.2);
        }

        .cta-secondary:hover {
            background: rgba(255, 255, 255, 0.25);
            border-color: rgba(255, 255, 255, 0.6);
            color: white;
            box-shadow: 
                0 6px 8px rgba(0, 0, 0, 0.4),
                0 15px 30px rgba(0, 0, 0, 0.3);
        }

        /* Emergency Service Button Override */
        .cta-button[href^="tel:"]:contains("Emergency"),
        a[href^="tel:"]:contains("Emergency Service") {
            background: linear-gradient(135deg, #dc2626, #c53030) !important;
            color: white !important;
            border: 3px solid #dc2626 !important;
            box-shadow: 
                0 0 20px rgba(220, 38, 38, 0.3),
                0 4px 6px rgba(15, 23, 42, 0.2) !important;
        }

        /* Emergency Service button targeting */
        a.cta-secondary[href^="tel:"] {
            background: linear-gradient(135deg, #dc2626, #c53030) !important;
            color: white !important;
            border: 3px solid #dc2626 !important;
            box-shadow: 
                0 0 20px rgba(220, 38, 38, 0.3),
                0 4px 6px rgba(15, 23, 42, 0.2) !important;
        }

        a.cta-secondary[href^="tel:"]:hover {
            background: linear-gradient(135deg, #b91c1c, #991b1b) !important;
            transform: translateY(-5px);
            box-shadow: 
                0 0 30px rgba(220, 38, 38, 0.5),
                0 8px 12px rgba(15, 23, 42, 0.25) !important;
        }

        /* Professional Authority Section */
        .authority {
            padding: 8rem 0;
            background: var(--professional-navy);
            color: white;
            position: relative;
            overflow: hidden;
        }

        .authority::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--metal-texture);
            background-size: 60px 60px;
            opacity: 0.02;
        }

        .authority h2 {
            text-align: center;
            font-family: 'Space Grotesk', sans-serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 700;
            margin-bottom: 1rem;
            background: linear-gradient(135deg, #ffffff 0%, #e2e8f0 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .authority-subtitle {
            text-align: center;
            font-size: 1.2rem;
            color: rgba(255, 255, 255, 0.8);
            margin-bottom: 6rem;
            font-weight: 300;
        }

        .certifications {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 2rem;
            margin-bottom: 6rem;
        }

        .cert-item {
            background: var(--glass-white);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 2rem 1.5rem;
            border-radius: 20px;
            text-align: center;
            transition: all 0.3s ease;
        }

        .cert-item:hover {
            transform: translateY(-10px);
            background: var(--glass-white-strong);
        }

        .cert-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            display: block;
        }

        .cert-title {
            font-size: 1.1rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
        }

        .cert-desc {
            font-size: 0.9rem;
            color: rgba(255, 255, 255, 0.7);
            line-height: 1.5;
        }

        .counter-grid {
            display: grid;
            grid-template-columns: repeat(4, 1fr);
            gap: 3rem;
            max-width: 1000px;
            margin: 0 auto 4rem;
        }

        .counter-item {
            text-align: center;
        }

        .counter-number {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 4rem;
            font-weight: 800;
            background: var(--emergency-gradient);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            display: block;
            line-height: 1;
        }

        .counter-label {
            font-size: 1.1rem;
            color: rgba(255, 255, 255, 0.9);
            font-weight: 500;
            margin-top: 1rem;
        }

        .brands-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 2rem;
            align-items: center;
        }

        .brand-item {
            background: var(--glass-white);
            backdrop-filter: blur(10px);
            border: 1px solid rgba(255, 255, 255, 0.1);
            padding: 1.5rem;
            border-radius: 15px;
            text-align: center;
            font-weight: 600;
            color: rgba(255, 255, 255, 0.9);
            transition: all 0.3s ease;
        }

        .brand-item:hover {
            background: var(--glass-white-strong);
            transform: translateY(-5px);
        }

        /* Premium Services Section */
        .services {
            padding: 10rem 0;
            background: linear-gradient(135deg, #ffffff 0%, #f8fafc 100%);
            position: relative;
        }

        .services::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--blueprint-pattern);
            background-size: 30px 30px;
            opacity: 0.02;
        }

        .services h2 {
            text-align: center;
            font-family: 'Space Grotesk', sans-serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 700;
            margin-bottom: 2rem;
            background: var(--trust-gradient);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.02em;
        }

        .services-subtitle {
            text-align: center;
            font-size: 1.3rem;
            color: var(--reliable-gray);
            margin-bottom: 6rem;
            font-weight: 400;
            max-width: 600px;
            margin-left: auto;
            margin-right: auto;
        }

        .services-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(380px, 1fr));
            gap: 2rem;
            max-width: 1400px;
            margin: 0 auto;
        }

        .service-card {
            background: var(--glass-white-strong);
            backdrop-filter: blur(25px);
            -webkit-backdrop-filter: blur(25px);
            border: 1px solid rgba(15, 23, 42, 0.08);
            padding: 2.5rem;
            border-radius: 24px;
            box-shadow: var(--shadow-premium);
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            position: relative;
            overflow: hidden;
            will-change: transform;
        }

        .service-card::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--trust-gradient);
            opacity: 0;
            transition: opacity 0.4s ease;
            border-radius: 24px;
        }

        .service-card:hover::before {
            opacity: 0.03;
        }

        .service-card:hover {
            transform: translateY(-15px);
            box-shadow: var(--shadow-hover);
            border-color: rgba(15, 23, 42, 0.15);
        }

        .service-icon {
            width: 80px;
            height: 80px;
            background: var(--emergency-gradient);
            border-radius: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2.5rem;
            margin-bottom: 2rem;
            color: white;
            position: relative;
            z-index: 2;
        }

        .service-card h3 {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.8rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: var(--professional-navy);
            position: relative;
            z-index: 2;
        }

        .service-description {
            font-size: 1.1rem;
            color: var(--reliable-gray);
            margin-bottom: 2rem;
            line-height: 1.7;
            position: relative;
            z-index: 2;
        }

        .service-features {
            list-style: none;
            margin-bottom: 2rem;
            position: relative;
            z-index: 2;
        }

        .service-features li {
            display: flex;
            align-items: center;
            gap: 0.8rem;
            margin-bottom: 0.8rem;
            font-size: 0.95rem;
            color: var(--reliable-gray);
        }

        .service-features li::before {
            content: '✓';
            background: var(--success-gradient);
            color: white;
            border-radius: 50%;
            width: 20px;
            height: 20px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.7rem;
            font-weight: bold;
            flex-shrink: 0;
        }

        .service-pricing {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 2rem;
            position: relative;
            z-index: 2;
        }

        .service-price {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.5rem;
            font-weight: 700;
            background: var(--emergency-gradient);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .response-time-badge {
            background: var(--success-gradient);
            color: white;
            padding: 0.4rem 0.8rem;
            border-radius: 15px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .service-cta {
            width: 100%;
            background: transparent;
            color: var(--trust-blue);
            border: 2px solid var(--trust-blue);
            padding: 1rem 2rem;
            border-radius: 30px;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            position: relative;
            z-index: 2;
            overflow: hidden;
            font-family: 'Inter', sans-serif;
        }

        .service-cta::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: var(--trust-blue);
            border-radius: 50%;
            transition: all 0.4s ease;
            transform: translate(-50%, -50%);
            z-index: -1;
        }

        .service-cta:hover::before {
            width: 400px;
            height: 400px;
        }

        .service-cta:hover {
            color: white;
            transform: translateY(-3px);
            box-shadow: var(--shadow-premium);
        }

        /* Premium Contact Section */
        .contact {
            padding: 10rem 0;
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            position: relative;
        }

        .contact::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--blueprint-pattern);
            background-size: 25px 25px;
            opacity: 0.02;
        }

        .contact h2 {
            text-align: center;
            font-family: 'Space Grotesk', sans-serif;
            font-size: clamp(2.5rem, 5vw, 4rem);
            font-weight: 700;
            margin-bottom: 2rem;
            background: var(--trust-gradient);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .contact-subtitle {
            text-align: center;
            font-size: 1.3rem;
            color: var(--reliable-gray);
            margin-bottom: 6rem;
            font-weight: 400;
        }

        .contact-container {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 4rem;
            max-width: 1200px;
            margin: 0 auto;
            align-items: stretch;
        }

        .contact-info {
            background: var(--glass-white-strong);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(15, 23, 42, 0.08);
            border-radius: 20px;
            padding: 3rem;
            box-shadow: var(--shadow-premium);
        }

        .contact-item {
            display: flex;
            align-items: center;
            gap: 1.5rem;
            margin-bottom: 2.5rem;
            padding: 1.5rem;
            background: rgba(255, 255, 255, 0.5);
            border-radius: 15px;
            transition: all 0.3s ease;
        }

        .contact-item:hover {
            transform: translateY(-3px);
            box-shadow: var(--shadow-floating);
        }

        .contact-icon {
            width: 60px;
            height: 60px;
            background: var(--emergency-gradient);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 1.5rem;
            color: white;
            flex-shrink: 0;
        }

        .contact-details h3 {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.2rem;
            font-weight: 700;
            color: var(--professional-navy);
            margin-bottom: 0.5rem;
        }

        .contact-details p {
            color: var(--reliable-gray);
            line-height: 1.6;
        }

        .contact-form {
            background: var(--glass-white-strong);
            backdrop-filter: blur(20px);
            border: 1px solid rgba(15, 23, 42, 0.08);
            padding: 3rem;
            border-radius: 20px;
            box-shadow: var(--shadow-premium);
        }

        .form-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .form-group {
            margin-bottom: 1.5rem;
        }

        .form-group.full-width {
            grid-column: 1 / -1;
        }

        .form-group label {
            display: block;
            margin-bottom: 0.8rem;
            font-weight: 600;
            color: var(--professional-navy);
            font-size: 0.95rem;
        }

        .form-group input,
        .form-group textarea,
        .form-group select {
            width: 100%;
            padding: 1.2rem;
            border: 2px solid rgba(15, 23, 42, 0.1);
            border-radius: 12px;
            font-size: 1rem;
            transition: all 0.3s ease;
            font-family: inherit;
            background: rgba(255, 255, 255, 0.9);
            backdrop-filter: blur(5px);
        }

        .form-group input:focus,
        .form-group textarea:focus,
        .form-group select:focus {
            outline: none;
            border-color: var(--emergency-red);
            transform: translateY(-2px);
            box-shadow: var(--shadow-floating);
            background: white;
        }

        .form-group textarea {
            height: 120px;
            resize: vertical;
        }

        .emergency-form-cta {
            width: 100%;
            background: linear-gradient(135deg, #1e40af, #1e3a8a);
            color: white;
            border: none;
            padding: 1.5rem 2rem;
            border-radius: 30px;
            font-size: 1.1rem;
            font-weight: 700;
            cursor: pointer;
            transition: all 0.3s ease;
            font-family: 'Inter', sans-serif;
            box-shadow: 0 10px 25px rgba(30, 64, 175, 0.3);
            position: relative;
            overflow: hidden;
        }

        .emergency-form-cta::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: var(--shimmer);
            transition: left 0.6s ease;
        }

        .emergency-form-cta:hover::before {
            left: 100%;
        }

        .emergency-form-cta:hover {
            transform: translateY(-3px);
            box-shadow: 0 30px 60px rgba(30, 64, 175, 0.4);
            background: linear-gradient(135deg, #1d4ed8, #1e40af);
        }

        /* Premium Footer */
        footer {
            background: linear-gradient(135deg, var(--professional-navy) 0%, #0f172a 100%);
            color: white;
            padding: 6rem 0 3rem;
            position: relative;
        }

        footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--metal-texture);
            background-size: 50px 50px;
            opacity: 0.02;
        }

        .footer-content {
            display: grid;
            grid-template-columns: 2fr 1fr 1fr 1fr;
            gap: 3rem;
            margin-bottom: 3rem;
            position: relative;
            z-index: 2;
        }

        .footer-brand {
            padding-right: 2rem;
        }

        .footer-logo {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 2rem;
            font-weight: 700;
            background: linear-gradient(135deg, #ffffff 0%, #e2e8f0 100%);
            background-clip: text;
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            margin-bottom: 1.5rem;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .footer-logo::before {
            content: '⚙';
            font-size: 1.5rem;
        }

        .footer-description {
            color: rgba(255, 255, 255, 0.8);
            line-height: 1.6;
            margin-bottom: 2rem;
        }

        .footer-emergency {
            background: var(--emergency-gradient);
            color: white;
            padding: 1rem 2rem;
            border-radius: 30px;
            text-decoration: none;
            font-weight: 700;
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            transition: all 0.3s ease;
        }

        .footer-emergency:hover {
            transform: translateY(-2px);
            box-shadow: var(--shadow-emergency);
        }

        .footer-section h3 {
            font-family: 'Space Grotesk', sans-serif;
            font-size: 1.2rem;
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: white;
        }

        .footer-section ul {
            list-style: none;
        }

        .footer-section li {
            margin-bottom: 0.8rem;
        }

        .footer-section a {
            color: rgba(255, 255, 255, 0.8);
            text-decoration: none;
            transition: all 0.3s ease;
        }

        .footer-section a:hover {
            color: white;
            transform: translateX(5px);
        }

        .footer-bottom {
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            padding-top: 2rem;
            text-align: center;
            position: relative;
            z-index: 2;
        }

        .footer-bottom p {
            color: rgba(255, 255, 255, 0.7);
            font-size: 0.9rem;
        }

        /* Mobile Responsive */
        @media (max-width: 1024px) {
            .container { padding: 0 1.5rem; }
            .services-grid { grid-template-columns: repeat(auto-fit, minmax(300px, 1fr)); }
            .contact-container { grid-template-columns: 1fr; gap: 3rem; }
            .footer-content { grid-template-columns: 1fr 1fr; }
        }

                 @media (max-width: 768px) {
             .container { padding: 0 1rem; }
             .header-content { flex-direction: column; gap: 1rem; }
             
             .hero { 
                 padding-top: 100px;
                 background: 
                     linear-gradient(135deg, 
                         rgba(255, 255, 255, 0.45) 0%, 
                         rgba(255, 255, 255, 0.35) 30%,
                         rgba(255, 255, 255, 0.25) 60%,
                         rgba(255, 255, 255, 0.35) 100%
                     ),
                     url('hvac acc/hvac hero.png');
                 background-size: cover, cover;
                 background-position: center center, center center;
                 background-attachment: scroll, scroll;
                 background-repeat: no-repeat;
             }
             
             .hero::before {
                 background: 
                     radial-gradient(circle at 50% 80%, rgba(255, 255, 255, 0.12) 0%, transparent 70%),
                     linear-gradient(180deg, rgba(255, 255, 255, 0.1) 0%, transparent 50%, rgba(255, 255, 255, 0.15) 100%);
             }
             
             .hero-stats { justify-content: center; }
             .services-grid { grid-template-columns: 1fr; }
             .certifications { grid-template-columns: repeat(2, 1fr); }
             .counter-grid { grid-template-columns: repeat(2, 1fr); gap: 2rem; }
             .footer-content { grid-template-columns: 1fr; }
             .floating-blueprint { display: none; }
         }

                 @media (max-width: 480px) {
             .hero h1 { font-size: 2.5rem; }
             .certifications { grid-template-columns: 1fr; }
             .counter-grid { grid-template-columns: 1fr; gap: 1.5rem; }
             .form-grid { grid-template-columns: 1fr; }
         }

        /* Professional HVAC Chatbot Styles */
        .hvac-chat-container {
            position: fixed;
            bottom: 2rem;
            right: 2rem;
            z-index: 9990;
            font-family: 'Inter', sans-serif;
        }

        .hvac-chat-toggle {
            width: 65px;
            height: 65px;
            background: linear-gradient(135deg, #1e40af 0%, #1e3a8a 100%);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            box-shadow: var(--shadow-premium), 0 0 0 3px rgba(30, 64, 175, 0.2);
            transition: all 0.3s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            border: 2px solid rgba(234, 88, 12, 0.2);
            position: relative;
            overflow: hidden;
        }

        .hvac-chat-toggle::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--emergency-gradient);
            opacity: 0;
            transition: opacity 0.3s ease;
            border-radius: 50%;
        }

        .hvac-chat-toggle:hover::before {
            opacity: 0.1;
        }

        .hvac-chat-toggle:hover {
            transform: translateY(-4px) scale(1.05);
            box-shadow: var(--shadow-premium), 0 0 20px rgba(30, 64, 175, 0.4);
        }

        .chat-icon, .chat-close {
            color: white;
            transition: all 0.3s ease;
            position: relative;
            z-index: 2;
            display: flex;
            align-items: center;
            justify-content: center;
            width: 100%;
            height: 100%;
        }

        .chat-close {
            display: none;
        }

        .hvac-chat-toggle.active .chat-icon {
            display: none;
        }

        .hvac-chat-toggle.active .chat-close {
            display: flex;
        }

        .hvac-chat-widget {
            position: absolute;
            bottom: 85px;
            right: 0;
            width: 350px;
            height: 450px;
            background: var(--glass-white-strong);
            backdrop-filter: blur(30px);
            -webkit-backdrop-filter: blur(30px);
            border: 1px solid rgba(30, 64, 175, 0.1);
            border-radius: 20px;
            box-shadow: var(--shadow-premium);
            display: flex;
            flex-direction: column;
            transform: translateY(20px) scale(0.95);
            opacity: 0;
            visibility: hidden;
            transition: all 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
            overflow: hidden;
        }

        .hvac-chat-widget.active {
            transform: translateY(0) scale(1);
            opacity: 1;
            visibility: visible;
        }

        .hvac-chat-header {
            background: linear-gradient(135deg, #1e40af 0%, #1e3a8a 100%);
            padding: 1.2rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
            border-radius: 20px 20px 0 0;
            position: relative;
        }

        .hvac-chat-header::before {
            content: '';
            position: absolute;
            top: 0;
            left: 0;
            right: 0;
            bottom: 0;
            background: var(--emergency-gradient);
            opacity: 0.05;
        }

        .hvac-agent-info {
            position: relative;
            z-index: 2;
        }

        .hvac-agent-details h4 {
            color: white;
            font-size: 1.1rem;
            font-weight: 700;
            margin: 0 0 0.3rem 0;
            font-family: 'Space Grotesk', sans-serif;
        }

        .hvac-agent-status {
            color: rgba(255, 255, 255, 0.9);
            font-size: 0.85rem;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 0.3rem;
        }

        .hvac-chat-minimize {
            background: transparent;
            border: none;
            color: rgba(255, 255, 255, 0.8);
            cursor: pointer;
            padding: 0.5rem;
            border-radius: 8px;
            transition: all 0.3s ease;
            position: relative;
            z-index: 2;
        }

        .hvac-chat-minimize:hover {
            background: rgba(255, 255, 255, 0.1);
            color: white;
        }

        .hvac-chat-messages {
            flex: 1;
            padding: 1rem;
            overflow-y: auto;
            scroll-behavior: smooth;
        }

        .hvac-chat-messages::-webkit-scrollbar {
            width: 4px;
        }

        .hvac-chat-messages::-webkit-scrollbar-track {
            background: transparent;
        }

        .hvac-chat-messages::-webkit-scrollbar-thumb {
            background: rgba(30, 64, 175, 0.3);
            border-radius: 2px;
        }

        .hvac-message {
            margin-bottom: 1rem;
            animation: hvacMessageSlideIn 0.4s cubic-bezier(0.25, 0.46, 0.45, 0.94);
        }

        .hvac-bot-message .hvac-message-content {
            background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
            border: 1px solid rgba(30, 64, 175, 0.1);
            padding: 0.9rem 1.1rem;
            border-radius: 16px 16px 16px 4px;
            max-width: 85%;
            position: relative;
            box-shadow: 0 2px 8px rgba(30, 64, 175, 0.08);
        }

        .hvac-user-message {
            text-align: right;
        }

        .hvac-user-message .hvac-message-content {
            background: linear-gradient(135deg, #1e40af 0%, #1e3a8a 100%);
            color: white;
            padding: 0.9rem 1.1rem;
            border-radius: 16px 16px 4px 16px;
            max-width: 85%;
            margin-left: auto;
            display: inline-block;
            box-shadow: 0 2px 8px rgba(30, 64, 175, 0.2);
        }

        .hvac-message-content p {
            margin: 0;
            line-height: 1.5;
            font-size: 0.95rem;
        }

        .hvac-message-time {
            font-size: 0.75rem;
            color: var(--reliable-gray);
            margin-top: 0.5rem;
            opacity: 0.7;
        }



        .hvac-chat-input-container {
            padding: 0.8rem 1rem;
            border-top: 1px solid rgba(30, 64, 175, 0.1);
        }

        .hvac-chat-input {
            display: flex;
            gap: 0.75rem;
            align-items: center;
        }

        .hvac-chat-input input {
            flex: 1;
            padding: 1rem;
            border: 2px solid rgba(30, 64, 175, 0.15);
            border-radius: 12px;
            font-size: 0.95rem;
            background: rgba(255, 255, 255, 0.9);
            transition: all 0.3s ease;
            font-family: inherit;
        }

        .hvac-chat-input input:focus {
            outline: none;
            border-color: #1e40af;
            background: white;
            transform: translateY(-2px);
            box-shadow: var(--shadow-floating);
        }

        .hvac-send-button {
            width: 44px;
            height: 44px;
            background: linear-gradient(135deg, #1e40af 0%, #1e3a8a 100%);
            border: none;
            border-radius: 12px;
            color: white;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .hvac-send-button:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 20px rgba(30, 64, 175, 0.3);
        }

        .hvac-typing-indicator {
            display: none;
            padding: 0 1rem 0.8rem;
            align-items: center;
            gap: 0.75rem;
            font-size: 0.85rem;
            color: var(--reliable-gray);
        }

        .hvac-typing-indicator.active {
            display: flex;
        }

        .hvac-typing-dots {
            display: flex;
            gap: 0.25rem;
        }

        .hvac-typing-dots .dot {
            width: 6px;
            height: 6px;
            background: #1e40af;
            border-radius: 50%;
            animation: hvacTypingBounce 1.4s infinite ease-in-out;
        }

        .hvac-typing-dots .dot:nth-child(1) { animation-delay: -0.32s; }
        .hvac-typing-dots .dot:nth-child(2) { animation-delay: -0.16s; }



        @keyframes hvacMessageSlideIn {
            from {
                opacity: 0;
                transform: translateY(20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }

        @keyframes hvacTypingBounce {
            0%, 80%, 100% {
                transform: scale(0);
                opacity: 0.5;
            }
            40% {
                transform: scale(1);
                opacity: 1;
            }
        }

        /* Mobile Responsive for HVAC Chat */
        @media (max-width: 768px) {
            .hvac-chat-container {
                bottom: 1rem;
                right: 1rem;
            }

            .hvac-chat-widget {
                width: calc(100vw - 2rem);
                height: 400px;
                right: -1rem;
            }
        }

        /* Performance & Accessibility */
        .service-card, .cta-button { will-change: transform; }
        
        @media (prefers-reduced-motion: reduce) {
            *, *::before, *::after {
                animation-duration: 0.01ms !important;
                animation-iteration-count: 1 !important;
                transition-duration: 0.01ms !important;
            }
            .floating-blueprint, .emergency-badge { animation: none; }
        }
    </style>
</head>
<body>
    <!-- Premium Preloader -->
    <div class="premium-preloader" id="preloader">
        <div class="preloader-content">
            <div class="hvac-spinner"></div>
            <div class="preloader-text">Premium HVAC Solutions Loading...</div>
        </div>
    </div>





    <!-- Floating Background Elements -->
    <div class="floating-elements">
        <div class="floating-blueprint"></div>
        <div class="floating-blueprint"></div>
        <div class="floating-blueprint"></div>
        <div class="floating-blueprint"></div>
    </div>

    <!-- Premium Header -->
    <header id="header">
        <div class="container">
            <div class="header-content">
                <div class="logo">{{businessName}}</div>
                <nav>
                    <ul>
                        <li><a href="#home">Home</a></li>
                        <li><a href="#services">Services</a></li>
                        <li><a href="#about">About</a></li>
                        <li><a href="#contact">Contact</a></li>
                    </ul>
                </nav>
            </div>
        </div>
    </header>

    <!-- Cinematic Hero Section -->
    <section class="hero reveal-element" id="home">
        <div class="hero-overlay"></div>
        <div class="container">
            <div class="hero-content">
                                 <div class="hero-badges">
                     <div class="hero-badge emergency-badge">⚡ 24/7 Emergency Available</div>
                     <div class="hero-badge">⌘ Licensed & Insured</div>
                     <div class="hero-badge">✓ 100% Satisfaction Guaranteed</div>
                     <div class="hero-badge">★ BBB A+ Rating</div>
                 </div>
                <h1>{{heroTitle}}</h1>
                <p>{{tagline}}</p>
                <div class="hero-stats">
                    <div class="stat-item">
                        <span class="stat-number" data-count="15">15</span>
                        <div class="stat-label">Years Experience</div>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number" data-count="10000">10000+</span>
                        <div class="stat-label">Customers Served</div>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number" data-count="99">99%</span>
                        <div class="stat-label">% Satisfaction Rate</div>
                    </div>
                    <div class="stat-item">
                        <span class="stat-number" data-count="45">45</span>
                        <div class="stat-label">Min Response Time</div>
                    </div>
                </div>
                <div class="hero-ctas">
                    <a href="#contact" class="cta-button">{{ctaText}}</a>
                    <a href="tel:{{businessPhone}}" class="cta-button cta-secondary">Emergency Service</a>
                </div>
            </div>
        </div>
    </section>

    <!-- Professional Authority Section -->
    <section class="authority" id="about">
        <div class="container">
            <div class="authority-content reveal-element">
                <h2>Professional Excellence</h2>
                <p class="authority-subtitle">Certified expertise you can trust for all your HVAC needs</p>
                
                                 <div class="certifications">
                     <div class="cert-item">
                         <span class="cert-icon">⌘</span>
                         <div class="cert-title">Licensed & Bonded</div>
                         <div class="cert-desc">Fully licensed contractor with comprehensive insurance coverage</div>
                     </div>
                     <div class="cert-item">
                         <span class="cert-icon">★</span>
                         <div class="cert-title">BBB A+ Rating</div>
                         <div class="cert-desc">Better Business Bureau accredited with highest rating</div>
                     </div>
                     <div class="cert-item">
                         <span class="cert-icon">✓</span>
                         <div class="cert-title">EPA Certified</div>
                         <div class="cert-desc">Environmental Protection Agency certified technicians</div>
                     </div>
                     <div class="cert-item">
                         <span class="cert-icon">⚙</span>
                         <div class="cert-title">NATE Certified</div>
                         <div class="cert-desc">North American Technician Excellence certified team</div>
                     </div>
                 </div>

                                 <div class="counter-grid">
                     <div class="counter-item">
                         <span class="counter-number" data-count="15">15</span>
                         <div class="counter-label">Years in Business</div>
                     </div>
                     <div class="counter-item">
                         <span class="counter-number" data-count="10000">10000+</span>
                         <div class="counter-label">Happy Customers</div>
                     </div>
                     <div class="counter-item">
                         <span class="counter-number" data-count="99">99%</span>
                         <div class="counter-label">% Satisfaction Rate</div>
                     </div>
                     <div class="counter-item">
                         <span class="counter-number" data-count="24">24</span>
                         <div class="counter-label">24-Hour Emergency Service</div>
                     </div>
                 </div>

                <div class="brands-grid">
                    <div class="brand-item">Carrier</div>
                    <div class="brand-item">Trane</div>
                    <div class="brand-item">Lennox</div>
                    <div class="brand-item">York</div>
                    <div class="brand-item">Rheem</div>
                    <div class="brand-item">Goodman</div>
                </div>
            </div>
        </div>
    </section>

    <!-- Premium Services Section -->
    <section class="services" id="services">
        <div class="container">
            <h2 class="reveal-element">Expert HVAC Services</h2>
            <p class="services-subtitle reveal-element">Professional heating, cooling, and air quality solutions with guaranteed satisfaction</p>
            <div class="services-grid reveal-element">
                                 <div class="service-card">
                     <div class="service-icon">⚡</div>
                     <h3>Emergency Repairs</h3>
                    <p class="service-description">24/7 emergency HVAC repairs with rapid response times to restore your comfort when you need it most.</p>
                    <ul class="service-features">
                        <li>24/7 Emergency Availability</li>
                        <li>45-Minute Average Response</li>
                        <li>Upfront Pricing</li>
                        <li>Licensed Technicians</li>
                    </ul>
                    <div class="service-pricing">
                        <div class="service-price">Starting at \$89</div>
                        <div class="response-time-badge">45min response</div>
                    </div>
                    <button class="service-cta">Call for Emergency Service</button>
                </div>
                
                                 <div class="service-card">
                     <div class="service-icon">⌂</div>
                     <h3>System Installation</h3>
                    <p class="service-description">Professional installation of new HVAC systems with energy-efficient options and comprehensive warranties.</p>
                    <ul class="service-features">
                        <li>Energy-Efficient Systems</li>
                        <li>Professional Installation</li>
                        <li>Comprehensive Warranties</li>
                        <li>Financing Available</li>
                    </ul>
                    <div class="service-pricing">
                        <div class="service-price">Free Estimates</div>
                        <div class="response-time-badge">Same day quote</div>
                    </div>
                    <button class="service-cta">Get Free Estimate</button>
                </div>
                
                                 <div class="service-card">
                     <div class="service-icon">⚙</div>
                     <h3>Maintenance Programs</h3>
                    <p class="service-description">Regular maintenance programs to keep your system running efficiently and prevent costly emergency repairs.</p>
                    <ul class="service-features">
                        <li>Bi-Annual Tune-ups</li>
                        <li>Priority Emergency Service</li>
                        <li>Discounted Repairs</li>
                        <li>Extended Equipment Life</li>
                    </ul>
                    <div class="service-pricing">
                        <div class="service-price">Starting at \$149</div>
                        <div class="response-time-badge">Scheduled service</div>
                    </div>
                    <button class="service-cta">Schedule Maintenance</button>
                </div>
            </div>
        </div>
    </section>

    <!-- Premium Contact Section -->
    <section class="contact" id="contact">
        <div class="container">
            <h2 class="reveal-element">Get Your Free Estimate</h2>
            <p class="contact-subtitle reveal-element">Ready to improve your comfort? Contact us for expert HVAC solutions.</p>
            <div class="contact-container reveal-element">
                <div class="contact-info">
                                         <div class="contact-item">
                         <div class="contact-icon">☎</div>
                         <div class="contact-details">
                             <h3>Emergency Service</h3>
                             <p>{{businessPhone}}<br>24/7 Emergency Availability</p>
                         </div>
                     </div>
                     <div class="contact-item">
                         <div class="contact-icon">◉</div>
                         <div class="contact-details">
                             <h3>Service Area</h3>
                             <p>{{businessAddress}}<br>Serving the entire metro area</p>
                         </div>
                     </div>
                     <div class="contact-item">
                         <div class="contact-icon">⧖</div>
                         <div class="contact-details">
                             <h3>Response Time</h3>
                             <p>Average 45 minutes<br>Same-day service available</p>
                         </div>
                     </div>
                     <div class="contact-item">
                         <div class="contact-icon">\$</div>
                         <div class="contact-details">
                             <h3>Free Estimates</h3>
                             <p>No hidden fees<br>Upfront, transparent pricing</p>
                         </div>
                     </div>
                </div>
                
                <form class="contact-form">
                    <div class="form-grid">
                        <div class="form-group">
                            <label for="name">Full Name</label>
                            <input type="text" id="name" name="name" required>
                        </div>
                        <div class="form-group">
                            <label for="phone">Phone Number</label>
                            <input type="tel" id="phone" name="phone" required>
                        </div>
                    </div>
                    <div class="form-group">
                        <label for="email">Email Address</label>
                        <input type="email" id="email" name="email" required>
                    </div>
                    <div class="form-group">
                        <label for="service">Service Needed</label>
                        <select id="service" name="service" required>
                            <option value="">Select a service...</option>
                            <option value="emergency">Emergency Repair</option>
                            <option value="installation">New Installation</option>
                            <option value="maintenance">Maintenance</option>
                            <option value="quote">Free Estimate</option>
                        </select>
                    </div>
                    <div class="form-group">
                        <label for="message">Describe Your HVAC Needs</label>
                        <textarea id="message" name="message" placeholder="Tell us about your heating, cooling, or air quality concerns..."></textarea>
                    </div>
                    <button type="submit" class="emergency-form-cta">{{ctaText}}</button>
                </form>
            </div>
        </div>
    </section>

    <!-- Premium Footer -->
    <footer>
        <div class="container">
            <div class="footer-content">
                <div class="footer-brand">
                    <div class="footer-logo">{{businessName}}</div>
                    <p class="footer-description">Professional HVAC services with 15+ years of experience. Licensed, insured, and committed to your comfort with 24/7 emergency service.</p>
                                         <a href="tel:{{businessPhone}}" class="footer-emergency">
                         ⚡ Emergency: {{businessPhone}}
                     </a>
                </div>
                <div class="footer-section">
                    <h3>Services</h3>
                    <ul>
                        <li><a href="#services">Emergency Repairs</a></li>
                        <li><a href="#services">System Installation</a></li>
                        <li><a href="#services">Maintenance</a></li>
                        <li><a href="#services">Air Quality</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h3>Company</h3>
                    <ul>
                        <li><a href="#about">About Us</a></li>
                        <li><a href="#about">Certifications</a></li>
                        <li><a href="#contact">Service Area</a></li>
                        <li><a href="#contact">Contact</a></li>
                    </ul>
                </div>
                <div class="footer-section">
                    <h3>Support</h3>
                    <ul>
                        <li><a href="#contact">Free Estimates</a></li>
                        <li><a href="#services">Financing</a></li>
                        <li><a href="#services">Warranties</a></li>
                        <li><a href="#contact">Emergency Service</a></li>
                    </ul>
                </div>
            </div>
            <div class="footer-bottom">
                <p>&copy; 2024 {{businessName}}. All rights reserved. Licensed & Insured HVAC Contractor.</p>
            </div>
        </div>
    </footer>

    <!-- Professional HVAC Chatbot -->
    <div class="hvac-chat-container" id="hvacChatContainer">
        <!-- Chat Toggle Button -->
        <div class="hvac-chat-toggle" id="hvacChatToggle">
            <div class="chat-icon">
                <svg width="28" height="28" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M20 2H4c-1.1 0-2 .9-2 2v12c0 1.1.9 2 2 2h4l4 4 4-4h4c1.1 0 2-.9 2-2V4c0-1.1-.9-2-2-2zm-2 12H6v-2h12v2zm0-3H6V9h12v2zm0-3H6V6h12v2z"/>
                </svg>
            </div>
            <div class="chat-close" id="hvacChatClose">
                <svg width="24" height="24" viewBox="0 0 24 24" fill="currentColor">
                    <path d="M19 6.41L17.59 5 12 10.59 6.41 5 5 6.41 10.59 12 5 17.59 6.41 19 12 13.41 17.59 19 19 17.59 13.41 12z"/>
                </svg>
            </div>
        </div>

        <!-- Chat Widget -->
        <div class="hvac-chat-widget" id="hvacChatWidget">
            <!-- Chat Header -->
            <div class="hvac-chat-header">
                <div class="hvac-agent-info">
                    <div class="hvac-agent-details">
                        <h4>{{businessName}} HVAC</h4>
                        <span class="hvac-agent-status">🚨 24/7 Emergency Service</span>
                    </div>
                </div>
                <button class="hvac-chat-minimize" id="hvacChatMinimize">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="currentColor">
                        <path d="M19 13H5v-2h14v2z"/>
                    </svg>
                </button>
            </div>

            <!-- Chat Messages -->
            <div class="hvac-chat-messages" id="hvacChatMessages">
                <div class="hvac-message hvac-bot-message">
                    <div class="hvac-message-content">
                        <p>Hello! I'm here to answer questions about our HVAC services. How can I help you today?</p>
                    </div>
                    <div class="hvac-message-time">Just now</div>
                </div>
            </div>

            <!-- Chat Input -->
            <div class="hvac-chat-input-container">
                <div class="hvac-chat-input">
                    <input type="text" id="hvacMessageInput" placeholder="Describe your HVAC issue...">
                    <button class="hvac-send-button" id="hvacSendButton">
                        <svg width="20" height="20" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M2.01 21L23 12 2.01 3 2 10l15 2-15 2z"/>
                        </svg>
                    </button>
                </div>
            </div>

            <!-- Typing Indicator -->
            <div class="hvac-typing-indicator" id="hvacTypingIndicator">
                <div class="hvac-typing-dots">
                    <div class="dot"></div>
                    <div class="dot"></div>
                    <div class="dot"></div>
                </div>
                <span>Technician typing...</span>
            </div>


        </div>
    </div>

    <!-- Advanced JavaScript -->
    <script>
        // Preloader
        window.addEventListener('load', () => {
            setTimeout(() => {
                const preloader = document.getElementById('preloader');
                preloader.style.opacity = '0';
                preloader.style.visibility = 'hidden';
            }, 1500);
        });

        // Header scroll effect
        window.addEventListener('scroll', () => {
            const header = document.getElementById('header');
            if (window.scrollY > 100) {
                header.classList.add('scrolled');
            } else {
                header.classList.remove('scrolled');
            }
        });

        // Reveal animations
        const revealElements = document.querySelectorAll('.reveal-element, .reveal-left, .reveal-right');
        const revealObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('revealed');
                }
            });
        }, { threshold: 0.1, rootMargin: '0px 0px -50px 0px' });

        revealElements.forEach(element => {
            revealObserver.observe(element);
        });

        // Counter numbers are now static (no animation)

        // Smooth scroll
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        // Form interactions
        const formInputs = document.querySelectorAll('input, textarea, select');
        formInputs.forEach(input => {
            input.addEventListener('focus', function() {
                this.parentElement.style.transform = 'translateY(-2px)';
            });
            
            input.addEventListener('blur', function() {
                this.parentElement.style.transform = 'translateY(0)';
            });
        });

        // Emergency tracking
        document.querySelectorAll('[href^="tel:"]').forEach(phoneLink => {
            phoneLink.addEventListener('click', function() {
                console.log('Emergency HVAC call initiated');
            });
        });

        console.log('Premium HVAC website loaded successfully');

        // Simple HVAC Chat Assistant
        class SimpleHVACChatAssistant {
            constructor() {
                this.isOpen = false;
                this.responses = {
                    welcome: "Hello! I'm here to answer questions about our HVAC services. How can I help you today?",
                    
                    // Simple Q&A responses
                    hours: "We provide 24/7 emergency service! Regular business hours are Monday-Friday 8AM-6PM, Saturday 9AM-4PM. For emergencies, call {{businessPhone}} anytime.",
                    emergency: "For heating or cooling emergencies, call {{businessPhone}} immediately. We offer 24/7 emergency service with same-day response. Emergency service starts at \$89.",
                    services: "We offer heating & cooling repair, new system installation, maintenance, air quality solutions, and 24/7 emergency service. How can we help you?",
                    installation: "We provide free estimates for new HVAC systems. High-efficiency systems available with financing options. Our team is licensed and insured.",
                    maintenance: "Regular maintenance prevents breakdowns and extends system life. Our tune-ups include cleaning, inspection, and efficiency testing. Starting at \$149.",
                    repair: "We repair all brands of heating and cooling systems. Licensed technicians provide same-day service for most repairs. Emergency service available 24/7.",
                    cost: "We provide upfront pricing with no hidden fees. Emergency service starts at \$89. New installations start around \$3,000. Maintenance starts at \$149.",
                    financing: "We offer flexible financing options with approved credit. Low monthly payments available. Same-day approval for qualified customers.",
                    area: "We serve the entire metro area with same-day service. Our service area covers a 50-mile radius from our main location.",
                    license: "We are fully licensed, bonded, and insured HVAC contractors with EPA certification and NATE-certified technicians. BBB A+ rating.",
                    
                    default: "For immediate help, please call {{businessPhone}}. We're here 24/7 for emergencies. What specific HVAC question can I help answer?"
                };
                
                this.init();
            }
            
            init() {
                this.bindEvents();
            }
            
            bindEvents() {
                const chatToggle = document.getElementById('hvacChatToggle');
                const chatMinimize = document.getElementById('hvacChatMinimize');
                const sendButton = document.getElementById('hvacSendButton');
                const messageInput = document.getElementById('hvacMessageInput');
                
                chatToggle.addEventListener('click', () => this.toggleChat());
                chatMinimize.addEventListener('click', () => this.closeChat());
                sendButton.addEventListener('click', () => this.sendMessage());
                messageInput.addEventListener('keypress', (e) => {
                    if (e.key === 'Enter') this.sendMessage();
                });
            }
            
            toggleChat() {
                if (this.isOpen) {
                    this.closeChat();
                } else {
                    this.openChat();
                }
            }
            
            openChat() {
                const chatWidget = document.getElementById('hvacChatWidget');
                const chatToggle = document.getElementById('hvacChatToggle');
                
                chatWidget.classList.add('active');
                chatToggle.classList.add('active');
                this.isOpen = true;
                
                // Focus on input
                setTimeout(() => {
                    document.getElementById('hvacMessageInput').focus();
                }, 400);
            }
            
            closeChat() {
                const chatWidget = document.getElementById('hvacChatWidget');
                const chatToggle = document.getElementById('hvacChatToggle');
                
                chatWidget.classList.remove('active');
                chatToggle.classList.remove('active');
                this.isOpen = false;
            }
            
            addMessage(content, isUser = false, delay = 0) {
                setTimeout(() => {
                    const messagesContainer = document.getElementById('hvacChatMessages');
                    const messageDiv = document.createElement('div');
                    messageDiv.className = \`hvac-message \${isUser ? 'hvac-user-message' : 'hvac-bot-message'}\`;
                    
                    const now = new Date();
                    const timeString = now.toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'});
                    
                    messageDiv.innerHTML = \`
                        <div class="hvac-message-content">
                            <p>\${content}</p>
                        </div>
                        <div class="hvac-message-time">\${timeString}</div>
                    \`;
                    
                    messagesContainer.appendChild(messageDiv);
                    messagesContainer.scrollTop = messagesContainer.scrollHeight;
                }, delay);
            }
            
            showTyping() {
                const typingIndicator = document.getElementById('hvacTypingIndicator');
                typingIndicator.classList.add('active');
            }
            
            hideTyping() {
                const typingIndicator = document.getElementById('hvacTypingIndicator');
                typingIndicator.classList.remove('active');
            }
            
            sendMessage() {
                const messageInput = document.getElementById('hvacMessageInput');
                const message = messageInput.value.trim();
                
                if (!message) return;
                
                this.addMessage(message, true);
                messageInput.value = '';
                
                this.showTyping();
                setTimeout(() => {
                    this.hideTyping();
                    this.processMessage(message);
                }, 1500);
            }
            
            processMessage(message) {
                const lowerMessage = message.toLowerCase();
                let response = '';
                
                // Simple keyword detection for basic Q&A
                if (lowerMessage.includes('hours') || lowerMessage.includes('open') || lowerMessage.includes('time')) {
                    response = this.responses.hours;
                } else if (lowerMessage.includes('emergency') || lowerMessage.includes('urgent') || lowerMessage.includes('no heat') || lowerMessage.includes('no ac') || lowerMessage.includes('broken')) {
                    response = this.responses.emergency;
                } else if (lowerMessage.includes('services') || lowerMessage.includes('what do you') || lowerMessage.includes('help')) {
                    response = this.responses.services;
                } else if (lowerMessage.includes('install') || lowerMessage.includes('new system') || lowerMessage.includes('replace')) {
                    response = this.responses.installation;
                } else if (lowerMessage.includes('maintenance') || lowerMessage.includes('tune') || lowerMessage.includes('check')) {
                    response = this.responses.maintenance;
                } else if (lowerMessage.includes('repair') || lowerMessage.includes('fix') || lowerMessage.includes('broken')) {
                    response = this.responses.repair;
                } else if (lowerMessage.includes('cost') || lowerMessage.includes('price') || lowerMessage.includes('how much')) {
                    response = this.responses.cost;
                } else if (lowerMessage.includes('financing') || lowerMessage.includes('payment') || lowerMessage.includes('monthly')) {
                    response = this.responses.financing;
                } else if (lowerMessage.includes('area') || lowerMessage.includes('location') || lowerMessage.includes('serve')) {
                    response = this.responses.area;
                } else if (lowerMessage.includes('license') || lowerMessage.includes('certified') || lowerMessage.includes('insured')) {
                    response = this.responses.license;
                } else if (lowerMessage.includes('hello') || lowerMessage.includes('hi') || lowerMessage.includes('hey')) {
                    response = this.responses.welcome;
                } else {
                    response = this.responses.default;
                }
                
                this.addMessage(response);
            }
            

        }
        
        // Initialize the simple HVAC chat assistant when page loads
        window.addEventListener('DOMContentLoaded', () => {
            new SimpleHVACChatAssistant();
        });
    </script>
</body>
</html>
    </style>
</head>
<body>
    <!-- Rest of the file remains unchanged -->
</body>
</html> `
};

// Load template function - USES EMBEDDED TEMPLATES
function loadTemplate(templateType) {
    const template = HTML_TEMPLATES[templateType];
    if (!template) {
        console.warn('Template ' + templateType + ' not found, using hvac as fallback');
        return HTML_TEMPLATES['hvac'];
    }
    return template;
}

// Main execution
const businessData = detectBusinessType(businessName, businessDescription);
let htmlTemplate = loadTemplate(businessData.template);

// Replace all placeholders
const finalHTML = htmlTemplate
    .replace(/{{businessName}}/g, businessName)
    .replace(/{{heroTitle}}/g, businessData.heroTitle)
    .replace(/{{tagline}}/g, businessData.tagline)
    .replace(/{{ctaText}}/g, businessData.ctaText)
    .replace(/{{businessAddress}}/g, businessAddress)
    .replace(/{{businessPhone}}/g, businessPhone)
    .replace(/{{primaryColor}}/g, businessData.primaryColor)
    .replace(/{{secondaryColor}}/g, businessData.secondaryColor)
    .replace(/{{backgroundColor}}/g, businessData.backgroundColor)
    .replace(/{{textColor}}/g, businessData.textColor)
    .replace(/{{accentColor}}/g, businessData.accentColor)
    .replace(/{{mapEmbedCode}}/g, '');

console.log('✅ COMPLETE Template loaded: ' + businessData.template);
console.log('✅ Industry detected: ' + businessData.industry);
console.log('✅ Chatbot selector: ' + businessData.chatbotSelector);

// Return the result array for n8n
return [{
    json: {
        html: finalHTML,
        industry: businessData.industry,
        template: businessData.template,
        businessName: businessName,
        businessPhone: businessPhone,
        chatbotSelector: businessData.chatbotSelector,
        inputSelector: businessData.inputSelector
    }
}];